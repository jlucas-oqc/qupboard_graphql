name: Reusable Test Workflow

on:
  workflow_call:
    inputs:
      python-version:
        required: true
        type: string

jobs:
  test:
    name: Test suite
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-in-project: true

      - name: Cache virtual environment
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ runner.os }}-py${{ inputs.python-version }}-${{ hashFiles('poetry.lock') }}
          restore-keys: |
            venv-${{ runner.os }}-py${{ inputs.python-version }}-

      - name: Install dependencies
        run: poetry install --with dev

      - name: Run tests with coverage
        run: |
          poetry run pytest \
            --tb=short \
            --junitxml=reports/junit-${{ inputs.python-version }}.xml \
            --cov=src/qupboard_graphql \
            --cov-report=term-missing \
            --cov-report=xml:reports/coverage-${{ inputs.python-version }}.xml \
            --cov-report=html:reports/htmlcov-${{ inputs.python-version }}

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-py${{ inputs.python-version }}
          path: reports/junit-${{ inputs.python-version }}.xml
          retention-days: 30

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-py${{ inputs.python-version }}
          path: |
            reports/coverage-${{ inputs.python-version }}.xml
            reports/htmlcov-${{ inputs.python-version }}/
          retention-days: 30
