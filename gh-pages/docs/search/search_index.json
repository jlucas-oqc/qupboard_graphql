{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Qupboard GraphQL / REST API","text":""},{"location":"#introduction","title":"Introduction","text":"<p>Qupboard is a proof-of-concept service for storing and serving hardware calibration models via both a GraphQL and a REST API. It is built with:</p> <ul> <li>FastAPI \u2013 HTTP framework for the REST and GraphQL routers</li> <li>Strawberry \u2013 GraphQL schema and query engine</li> <li>SQLAlchemy \u2013 ORM and database abstraction</li> <li>Alembic \u2013 database schema migrations</li> <li>Pydantic \u2013 request/response validation and serialisation</li> </ul> <p>The default backing store is SQLite (<code>qupboard.db</code>), configured via the <code>DATABASE_URL</code> environment variable, but because we use SQLAlchemy, many other database engines may be used (PostgreSQL, MySQL, MariaDB, etc.).</p>"},{"location":"#table-of-contents","title":"Table of Contents","text":"<ul> <li>Getting Started</li> <li>Example Queries</li> <li>GraphQL Client Access</li> <li>Project Structure</li> <li>Architecture</li> <li>Database Implementation</li> <li>Database Migrations with Alembic</li> <li>Known Limitations / Not Implemented</li> </ul>"},{"location":"#getting-started","title":"Getting Started","text":""},{"location":"#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.12 or 3.13</li> <li>Poetry (dependency and virtual environment management)</li> </ul>"},{"location":"#installation","title":"Installation","text":"<p>After cloning, install all dependencies (including dev dependencies) into a local virtual environment:</p> <pre><code>poetry install --with dev\n</code></pre> <p>The virtual environment is created in-project at <code>.venv/</code> (configured in <code>poetry.toml</code>). Omit <code>--with dev</code> for a runtime-only install (no test, lint, or documentation tools).</p>"},{"location":"#configuration","title":"Configuration","text":"<p>The application is configured via environment variables. All settings have sensible defaults so no configuration is required to run locally:</p> Variable Default Description <code>DATABASE_URL</code> <code>sqlite:///./qupboard.db</code> SQLAlchemy database URL <code>GRAPHQL_PATH</code> <code>/graphql</code> Path for the GraphQL endpoint <code>REST_PATH</code> <code>/rest</code> Path prefix for the REST endpoints <p>To override a setting, export it before running the application:</p> <pre><code>export DATABASE_URL=\"sqlite:///./my_custom.db\"\n</code></pre>"},{"location":"#database-setup","title":"Database setup","text":"<p>Before running the application for the first time, apply all Alembic migrations to initialise the database schema:</p> <pre><code>poetry run alembic upgrade head\n</code></pre>"},{"location":"#running-the-application","title":"Running the application","text":"<p>Start the server using the installed <code>qupboard</code> script:</p> <pre><code>poetry run qupboard\n</code></pre> <p>or alternatively, run the FastAPI app directly (assuming you are in the project root and have installed dependencies into a virtual environment):</p> <pre><code>./src/qupboard_graphql/main.py\n</code></pre> <p>The server starts on <code>http://0.0.0.0:8000</code>. The following endpoints are then available:</p> URL Description <code>http://localhost:8000/</code> Redirects to <code>/docs</code> <code>http://localhost:8000/graphql</code> GraphQL API + interactive GraphiQL IDE <code>http://localhost:8000/rest</code> REST API <code>http://localhost:8000/docs</code> OpenAPI / Swagger UI <code>http://localhost:8000/healthcheck</code> Liveness probe \u2013 returns <code>OK</code>"},{"location":"#running-the-tests","title":"Running the tests","text":"<p>The test suite uses pytest with an in-memory SQLite database, so no prior database setup is required.</p> <p>For each test function, <code>tests/conftest.py</code>:</p> <ul> <li>creates a fresh SQLite in-memory engine (using <code>StaticPool</code> so all connections share the same   in-memory database),</li> <li>registers the <code>_set_sqlite_pragma</code> listener to enforce foreign-key constraints,</li> <li>creates the schema with <code>Base.metadata.create_all(...)</code>,</li> <li>temporarily swaps both <code>qupboard_graphql.db.session.engine</code> and   <code>qupboard_graphql.db.session.session_factory</code> to point at the test engine,</li> <li>runs requests through the real <code>get_db</code> dependency (no FastAPI <code>dependency_overrides</code>),</li> <li>drops the schema and disposes the engine during teardown, restoring the original module globals.</li> </ul> <p>This keeps tests isolated while exercising the same dependency path used in production.</p> <p>Run all tests:</p> <pre><code>poetry run pytest\n</code></pre> <p>Run tests in parallel (faster):</p> <pre><code>poetry run pytest -n auto\n</code></pre> <p>Run with coverage:</p> <pre><code>poetry run pytest --cov=qupboard_graphql --cov-report=term-missing\n</code></pre>"},{"location":"#generating-the-documentation","title":"Generating the documentation","text":"<p>The project documentation is built with MkDocs using the Material theme. API reference pages are generated automatically from the in-code docstrings via mkdocstrings. All documentation dependencies are included in the dev dependency group and are installed automatically by <code>poetry install</code>.</p> <p>To start a live-reloading local documentation server:</p> <pre><code>poetry run mkdocs serve\n</code></pre> <p>The docs are then available at <code>http://127.0.0.1:8000</code>. The server watches for changes to both the Markdown source files in <code>docs/</code> and the Python source files in <code>src/</code>, and automatically rebuilds the site on any change.</p> <p>Note: MkDocs and the application server both default to port 8000. If you need to run both simultaneously, start MkDocs on a different port: <code>poetry run mkdocs serve --dev-addr 127.0.0.1:8001</code>.</p> <p>To produce a self-contained static site (output written to <code>site/</code>):</p> <pre><code>poetry run mkdocs build\n</code></pre> <p>The <code>site/</code> directory is excluded from version control via <code>.gitignore</code>. The static output can be served by any web server or deployed to any static hosting provider (GitHub Pages, GitLab Pages, S3, etc.).</p>"},{"location":"#code-style-pre-commit-hooks","title":"Code style &amp; pre-commit hooks","text":"<p>The project uses Ruff for linting and formatting, and pre-commit to enforce hygiene checks automatically on every commit. The hooks are defined in <code>.pre-commit-config.yaml</code> and include:</p> Hook Purpose <code>ruff</code> Lint and auto-fix Python source files <code>ruff-format</code> Format Python source files <code>mdformat</code> Format Markdown files (100-character wrap) <code>conventional-pre-commit</code> Enforce Conventional Commits message format General file hooks Trailing whitespace, YAML/TOML/JSON validity, merge-conflict markers, etc. <p>Install the hooks into your local clone (one-time setup, run after <code>poetry install</code>):</p> <pre><code>poetry run pre-commit install --install-hooks\npoetry run pre-commit install --hook-type commit-msg\n</code></pre> <p>The second command is required for the <code>conventional-pre-commit</code> hook, which runs at the <code>commit-msg</code> stage rather than the default <code>pre-commit</code> stage.</p> <p>To run all hooks manually against the entire codebase:</p> <pre><code>poetry run pre-commit run --all-files\n</code></pre> <p>To run only the linter/formatter without committing:</p> <pre><code>poetry run ruff check --fix .\npoetry run ruff format .\n</code></pre>"},{"location":"#example-queries","title":"Example Queries","text":""},{"location":"#rest","title":"REST","text":"<p>The REST API is available at <code>/rest</code>. Interactive OpenAPI docs are served at <code>/docs</code>.</p> Method Path Description <code>GET</code> <code>/healthcheck</code> Health check \u2013 returns <code>OK</code> <code>GET</code> <code>/rest/logical-hardware</code> List all hardware model UUIDs <code>GET</code> <code>/rest/logical-hardware/{uuid}</code> Fetch a hardware model by UUID <code>POST</code> <code>/rest/logical-hardware</code> Create a hardware model from a JSON body <code>POST</code> <code>/rest/logical-hardware/upload</code> Create a hardware model from an uploaded JSON file <p>Before running any read queries, you must upload at least one hardware model. Use the example included in the repository:</p> <pre><code>curl -X POST http://localhost:8000/rest/logical-hardware/upload \\\n     -F \"file=@tests/data/calibration_pydantic.json;type=application/json\"\n</code></pre> <p>The response will contain the UUID of the newly created model \u2014 use that UUID in the read queries below.</p> <p>List all hardware model IDs</p> <pre><code>curl http://localhost:8000/rest/logical-hardware\n</code></pre> <p>Fetch a specific hardware model</p> <pre><code>curl http://localhost:8000/rest/logical-hardware/&lt;uuid&gt;\n</code></pre> <p>Create a hardware model from a JSON body</p> <pre><code>curl -X POST http://localhost:8000/rest/logical-hardware \\\n     -H \"Content-Type: application/json\" \\\n     -d @path/to/calibration.json\n</code></pre> <p>Upload a hardware model from a file</p> <pre><code>curl -X POST http://localhost:8000/rest/logical-hardware/upload \\\n     -F \"file=@path/to/calibration.json;type=application/json\"\n</code></pre> <p>Upload multiple calibration files</p> <p>If you want to upload the same sample multiple times for local testing, use the UUID-stripped fixture at <code>tests/data/calibration_pydantic_no_uuids.json</code>:</p> <pre><code>for i in $(seq 1 5); do\n  curl -sS -X POST http://localhost:8000/rest/logical-hardware/upload \\\n       -F \"file=@tests/data/calibration_pydantic_no_uuids.json;type=application/json\"\n  echo\ndone\n</code></pre>"},{"location":"#graphql","title":"GraphQL","text":"<p>The GraphQL API is available at <code>/graphql</code>. An interactive GraphQL IDE is served at the same path in a browser.</p> <p>Before running any read queries, ensure you have uploaded at least one hardware model via the REST API (see above).</p> <p>List all stored hardware model IDs</p> <p>Use this query first to obtain a valid UUID to pass to <code>getCalibration</code>:</p> <pre><code>{\n  getAllHardwareModelIds\n}\n</code></pre> <p>Fetch a calibration by ID</p> <p>Pass a UUID returned by <code>getAllHardwareModelIds</code> as the <code>id</code> argument:</p> <pre><code>{\n  getCalibration(id: \"&lt;uuid&gt;\") {\n    id\n    version\n    calibrationId\n    logicalConnectivity\n    qubits {\n      edges {\n        node {\n          id\n          qubitKey\n          physicalChannel {\n            id\n            channelKind\n            basebandFrequency\n          }\n          pulseChannels {\n            edges {\n              node {\n                id\n                channelRole\n                frequency\n                pulse {\n                  id\n                  waveformType\n                  width\n                  amp\n                }\n              }\n            }\n          }\n          resonator {\n            id\n            pulseChannels {\n              edges {\n                node {\n                  id\n                  channelRole\n                  frequency\n                }\n              }\n            }\n          }\n        }\n      }\n    }\n  }\n}\n</code></pre> <p>For an exhaustive query that selects every available field (including <code>crossResonanceChannels</code>, <code>crossResonanceCancellationChannels</code>, <code>zxPi4Comps</code>, pulse sub-fields, etc.), see <code>test_get_calibration</code> in <code>tests/test_graphql.py</code>.</p> <p>Fetch the first page of qubits for a calibration</p> <p>The <code>qubits</code> field (and every other relationship collection) uses relay-style cursor pagination generated automatically by <code>strawberry-sqlalchemy-mapper</code>. Pass <code>first</code> to set the page size and <code>after</code> to advance to the next page using the <code>endCursor</code> returned in <code>pageInfo</code>.</p> <pre><code>{\n  getCalibration(id: \"&lt;uuid&gt;\") {\n    id\n    calibrationId\n    qubits(first: 5, after: null) {\n      edges {\n        cursor\n        node {\n          id\n          qubitKey\n        }\n      }\n      pageInfo {\n        hasNextPage\n        endCursor\n      }\n    }\n  }\n}\n</code></pre> <p>Take <code>endCursor</code> from the response and pass it as <code>after</code> to fetch the next page:</p> <pre><code>{\n  getCalibration(id: \"&lt;uuid&gt;\") {\n    qubits(first: 5, after: \"&lt;endCursor from previous response&gt;\") {\n      edges {\n        node { id qubitKey }\n      }\n      pageInfo {\n        hasNextPage\n        endCursor\n      }\n    }\n  }\n}\n</code></pre> <p>The full set of pagination arguments supported on <code>qubits</code> (and all other collections):</p> Argument Description <code>first</code> Page size going forward <code>after</code> Cursor from a previous <code>endCursor</code> \u2014 fetch the next page <code>last</code> Page size going backward <code>before</code> Cursor from a previous <code>startCursor</code> \u2014 fetch the previous page <p>Fetch all calibrations (first page)</p> <p><code>getAllCalibrations</code> uses the same relay-style cursor pagination as relationship fields. Pass <code>first</code>/<code>after</code> to page forward and <code>last</code>/<code>before</code> to page backward.</p> <pre><code>{\n  getAllCalibrations(first: 20) {\n    edges {\n      cursor\n      node {\n        id\n        version\n        calibrationId\n      }\n    }\n    pageInfo {\n      hasNextPage\n      endCursor\n    }\n  }\n}\n</code></pre> <p>To fetch the next page, pass <code>endCursor</code> from <code>pageInfo</code> as the <code>after</code> argument:</p> <pre><code>{\n  getAllCalibrations(first: 20, after: \"&lt;endCursor from previous response&gt;\") {\n    edges {\n      node { id calibrationId }\n    }\n    pageInfo {\n      hasNextPage\n      endCursor\n    }\n  }\n}\n</code></pre>"},{"location":"#graphql-client-access","title":"GraphQL Client Access","text":""},{"location":"#interactive-ide","title":"Interactive IDE","text":"<p>The easiest way to explore the schema and test queries is the interactive GraphiQL IDE served at <code>http://localhost:8000/graphql</code> in a browser.</p>"},{"location":"#downloading-the-schema","title":"Downloading the Schema","text":"<p>For programmatic access (e.g. for code generation or client development), there are several ways to obtain the schema.</p> <p>Option 1 \u2013 Strawberry CLI (recommended, no server required)</p> <pre><code>poetry run strawberry export-schema qupboard_graphql.api.graphql:schema\n</code></pre> <p>Option 2 \u2013 Introspection query via <code>curl</code></p> <pre><code>curl -s -X POST http://localhost:8000/graphql \\\n     -H \"Content-Type: application/json\" \\\n     -d '{\"query\": \"{ __schema { types { name } } }\"}' | jq\n</code></pre> <p>For the full schema definition:</p> <pre><code>curl -s -X POST http://localhost:8000/graphql \\\n     -H \"Content-Type: application/json\" \\\n     -d @- &lt;&lt;'EOF'\n{\n  \"query\": \"query IntrospectionQuery { __schema { queryType { name } mutationType { name } subscriptionType { name } types { ...FullType } directives { name description locations args { ...InputValue } } } } fragment FullType on __Type { kind name description fields(includeDeprecated: true) { name description args { ...InputValue } type { ...TypeRef } isDeprecated deprecationReason } inputFields { ...InputValue } interfaces { ...TypeRef } enumValues(includeDeprecated: true) { name description isDeprecated deprecationReason } possibleTypes { ...TypeRef } } fragment InputValue on __InputValue { name description type { ...TypeRef } defaultValue } fragment TypeRef on __Type { kind name ofType { kind name ofType { kind name ofType { kind name ofType { kind name ofType { kind name ofType { kind name } } } } } } }\"\n}\nEOF\n</code></pre>"},{"location":"#graphql-client-libraries","title":"GraphQL Client Libraries","text":"<p>The <code>/graphql</code> endpoint is standard GraphQL-over-HTTP. For client development, any GraphQL client library that supports schema introspection and query execution can be used. The following are some popular options:</p> Language Library Sync Async Introspection Notes Python gql \u2705 \u2705 \u2705 runtime <ul><li>Most widely used Python GraphQL client</li><li>Multiple transports: HTTP, WebSocket, aiohttp</li><li>Validates queries against the live schema locally before sending</li><li>No generated types</li></ul> Python strawberry-graphql \u2705 \u2705 \u274c <ul><li>Minimal built-in HTTP client</li><li>No transport abstraction or local schema validation</li><li>Convenient if Strawberry is already a dependency; otherwise prefer <code>gql</code></li></ul> Python ariadne-codegen \u2705 \u2705 \u2705 at codegen time <ul><li>Generates a fully typed, dataclass-based client from <code>.graphql</code> query files</li><li>Full IDE auto-complete and static analysis</li><li>Requires a code-generation step; generated code must be kept in sync with the schema</li></ul> JavaScript Apollo Client \u274c \u2705 \u2705 via DevTools <ul><li>De-facto standard for GraphQL in the browser</li><li>Built-in normalised cache, reactive updates</li><li>Primarily React-oriented; heavier dependency for non-UI use</li></ul> JavaScript graphql-request \u2705 \u2705 \u274c <ul><li>Minimal zero-dependency client; ideal for Node.js scripts and serverless functions</li><li>No caching or schema validation</li></ul> TypeScript graphql-codegen \u2705 \u2705 \u2705 at codegen time <ul><li>Generates fully typed query hooks / SDK from <code>.graphql</code> files</li><li>Pluggable: outputs React hooks, plain fetch, urql, etc.</li><li>Requires a code-generation step; generated code must be kept in sync with the schema</li></ul> TypeScript urql \u274c \u2705 \u2705 via exchanges <ul><li>Lightweight, framework-agnostic (React, Vue, Svelte)</li><li>Extensible via exchanges (normalised cache, schema awareness, auth)</li><li>Smaller bundle than Apollo Client</li></ul> Rust cynic \u2705 \u2705 \u2705 at codegen time <ul><li>Generates strongly typed Rust structs from <code>.graphql</code> query files at compile time</li><li>Zero-cost schema validation; incompatible queries are compile errors</li><li>Requires a code-generation step</li></ul> Rust graphql-client \u2705 \u2705 \u2705 at codegen time <ul><li>Derive-macro based; generates request/response types from <code>.graphql</code> files</li><li>Works with any HTTP client (reqwest, surf, etc.)</li><li>Requires a code-generation step</li></ul> C++ CaffQL \u2705 \u274c \u2705 at codegen time <ul><li>Generates C++ types from a GraphQL schema and query files</li><li>The most feature-complete dedicated C++ option, but largely unmaintained since 2020</li><li>Requires a code-generation step</li></ul> C++ libcurl / cpr + manual \u2705 \u2705 \u274c <ul><li>No actively maintained, general-purpose C++ GraphQL client library exists</li><li>Typical approach: POST JSON with libcurl or cpr and parse the response with nlohmann/json or similar</li><li>No schema validation or generated types without additional tooling</li></ul>"},{"location":"#project-structure","title":"Project Structure","text":"<pre><code>qupboard_graphql/\n\u251c\u2500\u2500 pyproject.toml                  # Project metadata and dependencies\n\u251c\u2500\u2500 poetry.toml                     # Poetry local venv configuration\n\u251c\u2500\u2500 alembic.ini                     # Alembic configuration\n\u251c\u2500\u2500 alembic/\n\u2502   \u251c\u2500\u2500 env.py                      # Migration environment (wired to ORM models)\n\u2502   \u2514\u2500\u2500 versions/                   # Generated migration scripts\n\u251c\u2500\u2500 qupboard.db                     # Default SQLite database\n\u251c\u2500\u2500 src/\n\u2502   \u2514\u2500\u2500 qupboard_graphql/\n\u2502       \u251c\u2500\u2500 main.py                 # Entrypoint \u2013 starts uvicorn on 0.0.0.0:8000\n\u2502       \u251c\u2500\u2500 config.py               # Pydantic settings (DATABASE_URL, API paths)\n\u2502       \u251c\u2500\u2500 api/\n\u2502       \u2502   \u251c\u2500\u2500 app.py              # FastAPI application factory\n\u2502       \u2502   \u251c\u2500\u2500 graphql_types.py    # Strawberry type declarations (StrawberrySQLAlchemyMapper + @mapper.type classes)\n\u2502       \u2502   \u251c\u2500\u2500 graphql.py          # Query resolvers, schema, and GraphQL router\n\u2502       \u2502   \u251c\u2500\u2500 rest.py             # REST router (CRUD for hardware models)\n\u2502       \u2502   \u2514\u2500\u2500 root.py             # Health-check and root redirect routes\n\u2502       \u251c\u2500\u2500 db/\n\u2502       \u2502   \u251c\u2500\u2500 database.py         # SQLAlchemy DeclarativeBase\n\u2502       \u2502   \u251c\u2500\u2500 repository.py       # RepositoryMixin (get_by_uuid, get_all_pks)\n\u2502       \u2502   \u251c\u2500\u2500 models.py           # ORM models mirroring the hardware model schema\n\u2502       \u2502   \u251c\u2500\u2500 mapper_from_orm.py  # ORM \u2192 Pydantic conversion helpers\n\u2502       \u2502   \u251c\u2500\u2500 mapper_to_orm.py    # Pydantic \u2192 ORM conversion helpers\n\u2502       \u2502   \u2514\u2500\u2500 session.py          # get_engine() factory, module-level engine/session_factory, and get_db dependency\n\u2502       \u2514\u2500\u2500 schemas/\n\u2502           \u2514\u2500\u2500 hardware_model.py   # Pydantic schema for HardwareModel\n\u2514\u2500\u2500 tests/\n    \u251c\u2500\u2500 conftest.py\n    \u251c\u2500\u2500 test_graphql.py\n    \u251c\u2500\u2500 test_model_loader.py\n    \u251c\u2500\u2500 test_rest.py\n    \u251c\u2500\u2500 test_root.py\n    \u2514\u2500\u2500 data/                       # Sample calibration JSON fixtures\n</code></pre>"},{"location":"#architecture","title":"Architecture","text":""},{"location":"#layers","title":"Layers","text":"<p>The application is split into four distinct layers, each with a clear responsibility and minimal coupling to the others:</p> <ul> <li>API layer (<code>api/</code>) \u2013 FastAPI routers for REST and GraphQL endpoints, plus the GraphQL schema   and resolvers.</li> <li>Schema layer (<code>schemas/</code>) \u2013 Pydantic models defining the hardware model schema used for   request validation and serialisation in the REST API.</li> <li>Database layer (<code>db/</code>) \u2013 SQLAlchemy ORM models, database session management, and mapping   helpers to convert between the Pydantic schema and the ORM models.</li> <li>DB Engine \u2013 the actual database engine (SQLite by default, but configurable via   <code>DATABASE_URL</code>).</li> </ul> <p>Below is a high-level architecture diagram showing how these layers interact for both REST and GraphQL. In this PoC we provide Pydantic models and manual mappers to demonstrate how a REST API can be decoupled from the database schema (and indeed The REST endpoints are used to import real, legacyQat-format calibration files), while the GraphQL API maps directly from the ORM models via <code>strawberry-sqlalchemy-mapper</code>, tracking the database schema without a separate translation layer.</p> <pre><code>flowchart TD\n    Client([\"HTTP Client\"])\n\n    subgraph fastAPI [\"FastAPI app\"]\n        subgraph routers [\"Endpoint routers\"]\n            GQL[\"graphql.py + graphql_types.py\"]\n            REST[\"rest.py\"]\n        end\n        subgraph pydantic [\"Legacy Pydantic schema\"]\n            HM[\"hardware_model.py&lt;br/&gt;(Pydantic models)\"]\n            TO[\"mapper_to_orm.py&lt;br/&gt;Pydantic \u2192 ORM\"]\n            FROM[\"mapper_from_orm.py&lt;br/&gt;ORM \u2192 Pydantic\"]\n        end\n\n        ORM[\"(SQLAlchemy ORM)&lt;br/&gt;models.py&lt;br/&gt;repository.py&lt;br/&gt;session.py\"]\n    end\n\n    DB[(\"Database&lt;br/&gt;(SQLite / PostgreSQL / ...)\")]\n\n    Client &lt;--&gt; REST\n    Client &lt;--&gt; GQL\n\n    REST &lt;--&gt; HM\n    HM --&gt; TO\n    TO --&gt;|\"write\"| ORM\n\n    ORM --&gt;|\"read\"| FROM\n    FROM --&gt; HM\n\n    GQL --&gt;|\"reads directly&lt;br/&gt;(strawberry-sqlalchemy-mapper)\"| ORM\n    ORM &lt;--&gt; DB\n\n    style fastAPI fill:#ddeeff,stroke:#336699</code></pre>"},{"location":"#request-flow","title":"Request flow","text":"<p>REST write (<code>POST /rest/logical-hardware</code>):</p> <ol> <li>FastAPI parses the JSON body into a Pydantic <code>HardwareModel</code>.</li> <li><code>mapper_to_orm</code> converts it into a tree of SQLAlchemy ORM objects.</li> <li>The ORM objects are committed to the database and the new UUID is returned.</li> </ol> <p>REST read (<code>GET /rest/logical-hardware/{uuid}</code>):</p> <ol> <li><code>HardwareModelORM.get_by_uuid</code> fetches the row (and all related rows via eager-loaded    relationships).</li> <li><code>mapper_from_orm</code> reconstructs the full Pydantic <code>HardwareModel</code> and FastAPI serialises it to    JSON.</li> </ol> <p>GraphQL read (<code>getCalibration</code>, <code>getAllCalibrations</code>):</p> <ol> <li>Strawberry calls the resolver, which fetches the ORM row(s) directly.</li> <li><code>strawberry-sqlalchemy-mapper</code> translates the ORM objects into Strawberry types on the fly \u2014 no    manual mapping step required.</li> </ol> <p>The GraphQL path therefore bypasses the Pydantic <code>schemas/</code> layer entirely; the Pydantic layer is only used by the REST API and the ORM mappers. This means the database schema is effectively the source of truth for the GraphQL API: any change to the ORM models is immediately reflected in the GraphQL schema, with no separate mapping step required.</p> <p>The REST API is more decoupled from the database schema \u2014 the Pydantic models and manual mappers act as an explicit translation layer, giving the REST interface the freedom to evolve independently of the underlying data model. This makes the REST surface better suited to a stable, versioned contract for existing clients, while the GraphQL interface can track the database schema more closely and change more rapidly.</p> <p>Note that <code>strawberry-sqlalchemy-mapper</code> generates Strawberry types directly from the ORM models and wires up relationship fields as paginated connections (hence the <code>edges { node { \u2026 } }</code> shape seen in the example queries). Generic field-level filtering is not provided automatically; any specialised queries (for example, fetching a subset of the qubits filtered by fidelity belonging to a particular QPU) would still be written as custom resolvers that return ORM objects in the same connection-style shape, leaving clients free to select whichever fields they need.</p>"},{"location":"#database-implementation","title":"Database Implementation","text":""},{"location":"#schema","title":"Schema","text":"<p>The database schema semi-mirrors the <code>HardwareModel</code> Pydantic schema and is defined as SQLAlchemy ORM models in <code>src/qupboard_graphql/db/models.py</code>. The table hierarchy is:</p> <pre><code>hardware_models\n\u2514\u2500\u2500 qubits                     (phase_comp_x_pi_2 inlined)\n    \u251c\u2500\u2500 physical_channels      (channel_kind = 'qubit'; baseband + IQ-bias inlined)\n    \u251c\u2500\u2500 pulse_channels         (channel_role discriminator; qubit-owned channels only)\n    \u2502   \u2502   channel_role values:\n    \u2502   \u2502     'drive'            \u2013 qubit drive channel\n    \u2502   \u2502     'second_state'     \u2013 second-state channel (ss_active, ss_delay)\n    \u2502   \u2502     'freq_shift'       \u2013 freq-shift channel   (fs_active, fs_amp, fs_phase)\n    \u2502   \u2502     'reset_qubit'      \u2013 qubit reset channel  (reset_delay)\n    \u2502   \u2502   pulse_role values in calibratable_pulses:\n    \u2502   \u2502     'drive'            \u2013 primary drive pulse\n    \u2502   \u2502     'drive_x_pi'       \u2013 X-\u03c0 drive pulse\n    \u2502   \u2502     'second_state'     \u2013 second-state pulse\n    \u2502   \u2502     'reset_qubit'      \u2013 qubit reset pulse\n    \u2502   \u2514\u2500\u2500 calibratable_pulses (owner_uuid -&gt; pulse_channels.id + pulse_role discriminator)\n    \u251c\u2500\u2500 cross_resonance_channels (role = 'cr' | 'crc')\n    \u2502   \u2514\u2500\u2500 calibratable_pulses  (pulse_role = 'cr'; crc channels have no pulse)\n    \u251c\u2500\u2500 zx_pi_4_comps          (one per CR pair)\n    \u2502   \u2514\u2500\u2500 calibratable_pulses (pulse_role = 'zx_precomp' | 'zx_postcomp', nullable)\n    \u2514\u2500\u2500 resonators             (one-to-one with qubit)\n        \u251c\u2500\u2500 physical_channels  (channel_kind = 'resonator'; baseband + IQ-bias inlined)\n        \u2514\u2500\u2500 pulse_channels     (channel_role discriminator; resonator-owned channels only)\n            \u2502   channel_role values:\n            \u2502     'measure'        \u2013 resonator measure channel\n            \u2502     'acquire'        \u2013 resonator acquire channel (acq_delay/width/sync/use_weights)\n            \u2502     'reset_resonator'\u2013 resonator reset channel   (reset_delay)\n            \u2502   pulse_role values in calibratable_pulses:\n            \u2502     'measure'        \u2013 measure pulse\n            \u2502     'reset_resonator'\u2013 resonator reset pulse\n            \u2514\u2500\u2500 calibratable_pulses (owner_uuid -&gt; pulse_channels.id + pulse_role discriminator)\n</code></pre> <p>Note that although this does store the same information as the <code>HardwareModel</code> schema, it is not a direct 1:1 mapping \u2014 we have made some adjustments. For example, ids are consistently generated as UUIDs at the database level (instead of a mix of UUIDs and integer PKs) and named <code>id</code>, and some fields are inlined for simplicity (e.g. physical channel parameters are stored directly on the qubits and resonators instead of separate tables).</p> <p>This is part of the demonstration to show how legacy schemas can be adapted to fit the needs of a particular database engine or query patterns, and how the REST API can be decoupled from the database schema via the Pydantic layer and manual mappers.</p> <p>Exposing the SQLAlchemy models directly in the GraphQL layer via <code>strawberry-sqlalchemy-mapper</code> is also a deliberate choice to show how the GraphQL API can track the database schema closely without needing a separate mapping layer, while the REST API can evolve more independently to keep legacy clients happy.</p> <p>The database URL defaults to <code>sqlite:///./qupboard.db</code> and can be overridden with the <code>DATABASE_URL</code> environment variable.</p>"},{"location":"#database-migrations-with-alembic","title":"Database Migrations with Alembic","text":"<p>As the application evolves, the database schema needs to evolve with it \u2014 new tables are added, columns are renamed, types change, and so on. Without a migration tool, keeping the schema in sync across development machines, CI environments, and production deployments requires manual <code>ALTER TABLE</code> statements that are error-prone and hard to reproduce reliably.</p> <p>Alembic solves this by treating schema changes as versioned migration scripts stored alongside the source code. Each migration is a small Python file describing how to move the schema forward (<code>upgrade</code>) and, optionally, how to reverse that change (<code>downgrade</code>). Alembic records which migrations have been applied in an <code>alembic_version</code> table in the database, so it always knows exactly what state the schema is in and which scripts still need to run.</p> <p>Alembic integrates directly with SQLAlchemy, meaning it can autogenerate migration scripts by comparing the current ORM model definitions against the live database schema \u2014 catching additions, removals, and type changes automatically, though the generated scripts should always be reviewed before committing.</p> <p>The Alembic project lives at the project root:</p> <pre><code>alembic.ini          # Alembic configuration\nalembic/\n\u251c\u2500\u2500 env.py           # Wired to ORM Base metadata and app settings\n\u2514\u2500\u2500 versions/        # Migration scripts\n</code></pre> <p><code>env.py</code> automatically reads <code>DATABASE_URL</code> from the application settings, so no manual URL configuration is required. <code>render_as_batch=True</code> is enabled to support SQLite's limited <code>ALTER TABLE</code> capabilities.</p> <p>All commands below assume they are run from the project root.</p> <p>Apply all pending migrations</p> <pre><code>poetry run alembic upgrade head\n</code></pre> <p>Check whether the database is up to date</p> <pre><code>poetry run alembic check\n</code></pre> <p>Show the current revision</p> <pre><code>poetry run alembic current\n</code></pre> <p>Generate a new migration after changing ORM models</p> <pre><code>poetry run alembic revision --autogenerate -m \"describe_your_change\"\n</code></pre> <p>Downgrade one revision</p> <pre><code>poetry run alembic downgrade -1\n</code></pre> <p>Stamp an existing database without running migrations (useful when the schema was created outside of Alembic)</p> <pre><code>poetry run alembic stamp head\n</code></pre>"},{"location":"#known-limitations-not-implemented","title":"Known Limitations / Not Implemented","text":"<p>This is a proof-of-concept service. The following are notable omissions that would be required in a production-grade version:</p>"},{"location":"#n1-queries-on-the-rest-path","title":"N+1 queries on the REST path","text":"<p>The GraphQL API resolves relationships via <code>strawberry-sqlalchemy-mapper</code>'s built-in <code>StrawberrySQLAlchemyLoader</code>, which is a DataLoader \u2014 it batches all relationship loads for a given field across the whole result set into a single <code>SELECT \u2026 WHERE fk IN (\u2026)</code> query per level. The GraphQL path has no N+1 problem.</p> <p>The REST <code>GET /rest/logical-hardware/{uuid}</code> endpoint, however, loads a single hardware model and passes it to <code>mapper_from_orm</code>, which traverses the full relationship tree synchronously. The current eager-loading strategy (<code>lazy=\"subquery\"</code> on the CR channel relationships) avoids the worst cases but is not optimal. A production service should use explicit <code>selectinload</code> chains on the <code>get_by_uuid</code> query or set <code>lazy=\"selectin\"</code> on all relationships traversed by the REST mapper.</p>"},{"location":"#authentication-authorisation","title":"Authentication &amp; Authorisation","text":"<p>There is no authentication or authorisation. A production service would require at minimum:</p> <ul> <li>Authentication \u2013 e.g. OAuth 2.0 / OIDC JWT bearer tokens, API keys, or mutual TLS.</li> <li>Authorisation \u2013 role-based or attribute-based access control to restrict which clients can   read or write which hardware models.</li> </ul>"},{"location":"#graphql-mutations","title":"GraphQL Mutations","text":"<p>The GraphQL API is currently read-only. A real service would expose mutations for creating, updating, and deleting hardware models, with appropriate input validation mirroring what the REST <code>POST</code> endpoint does today.</p>"},{"location":"#input-validation-on-the-graphql-layer","title":"Input Validation on the GraphQL Layer","text":"<p>Pydantic validation is only applied on the REST path. GraphQL mutations (once added) should perform equivalent validation. Strawberry supports Pydantic integration via <code>strawberry.experimental.pydantic</code> that could be used to share the schema definitions.</p>"},{"location":"#async-database-access","title":"Async Database Access","text":"<p>The application uses a synchronous SQLAlchemy session. FastAPI runs synchronous dependencies in a thread pool, but this still blocks a worker thread per request under concurrent load. A production service should use SQLAlchemy's async engine and <code>AsyncSession</code> throughout.</p> <p>The conversion has been validated end-to-end on this codebase \u2014 all 22 tests pass with the async stack \u2014 and is straightforward. Only 6 source files and 2 test files change; the ORM model structure, Pydantic\u2194ORM mappers, Strawberry type definitions, app factory, and Alembic are all untouched. The key steps are:</p> <ul> <li>Add an async-compatible database driver (<code>aiosqlite</code> for SQLite, <code>asyncpg</code> for PostgreSQL) and   update the <code>DATABASE_URL</code> scheme to match.</li> <li>Replace the synchronous engine, session factory, and <code>get_db</code> dependency with their async   equivalents. Note that the SQLite foreign-key pragma listener must be attached to the underlying   sync engine, not the async wrapper.</li> <li>Update the repository methods to use SQLAlchemy 2.0-style <code>select()</code> queries (the legacy   <code>session.query()</code> API is not supported on async sessions). Add an optional eager-load parameter to   <code>get_by_uuid</code> so callers can request relationship loading without a separate method.</li> <li>In the REST layer, await all database calls and pass an explicit eager-load chain for the full   qubit relationship tree when fetching by UUID \u2014 <code>mapper_from_orm</code> traverses those relationships   synchronously on the objects it receives, so they must already be loaded before the session   closes.</li> <li>In the GraphQL layer, make all resolvers async. <code>strawberry-sqlalchemy-mapper</code> v0.8+ supports   async sessions natively and requires no changes to the type definitions. The loader must be   configured to reuse the request-scoped session rather than open new ones \u2014 if it opens fresh   sessions, the DataLoader queries hit a different connection, which breaks in-memory test databases   and bypasses the request transaction.</li> <li>Audit all ORM relationship declarations for async-unsafe lazy-loading strategies. <code>subquery</code>   loading is not async-safe and must be replaced. Any relationship traversed by <code>mapper_from_orm</code>   must be eagerly loaded, either via the relationship declaration or the explicit load chain above.   Take care not to eagerly load the top-level <code>qubits</code> collection on <code>HardwareModelORM</code> \u2014 doing so   causes the DataLoader to bypass its pagination logic, silently breaking cursor pagination on that   field.</li> <li>Update test fixtures to use the async engine and session equivalents. Tests that interact with the   app only via <code>TestClient</code> need no changes \u2014 <code>TestClient</code> handles async apps transparently.</li> </ul>"},{"location":"#caching","title":"Caching","text":"<p>Frequently-read calibration models are re-fetched from the database on every request. A caching layer (e.g. Redis with an appropriate TTL, or an in-process LRU cache invalidated on write) would dramatically reduce database load for read-heavy workloads.</p>"},{"location":"#observability","title":"Observability","text":"<p>There is no structured logging, metrics, or distributed tracing. A production service should emit:</p> <ul> <li>Structured logs (e.g. via <code>structlog</code>) including request IDs for correlation.</li> <li>Metrics (e.g. Prometheus counters/histograms via <code>prometheus-fastapi-instrumentator</code>).</li> <li>Traces (e.g. OpenTelemetry spans covering HTTP requests and database queries).</li> </ul>"},{"location":"#database-connection-pooling-configuration","title":"Database Connection Pooling &amp; Configuration","text":"<p>The current SQLite default is not suitable for production. When switching to PostgreSQL or another server-side database, connection pool settings (<code>pool_size</code>, <code>max_overflow</code>, <code>pool_timeout</code>) should be tuned and exposed via configuration, and a connection health-check (<code>pool_pre_ping=True</code>) should be enabled.</p>"},{"location":"#rest-api-versioning","title":"REST API Versioning","text":"<p>The REST endpoints have no version prefix (e.g. <code>/v1/rest/\u2026</code>). A stable public API should be versioned from the start to allow breaking changes to be introduced without disrupting existing clients.</p>"},{"location":"#error-handling","title":"Error Handling","text":"<p>Unhandled exceptions propagate as generic 500 responses with minimal detail. A real service would map domain errors (e.g. \"model not found\", \"validation failed\", \"database constraint violated\") to well-structured error responses with appropriate HTTP status codes, and equivalent GraphQL error extensions for the GraphQL path.</p>"},{"location":"config/","title":"Configuration","text":""},{"location":"config/#qupboard_graphql.config","title":"<code>qupboard_graphql.config</code>","text":"<p>Application configuration loaded from environment variables.</p> <p>Settings are read by Pydantic-Settings and fall back to sensible defaults so that the service starts without any environment configuration.</p>"},{"location":"config/#qupboard_graphql.config.Settings","title":"<code>Settings</code>","text":"<p>               Bases: <code>BaseSettings</code></p> <p>Application-wide settings resolved from environment variables.</p> <p>Attributes:</p> Name Type Description <code>GRAPHQL_PATH</code> <code>str</code> <p>URL prefix at which the GraphQL endpoint is mounted.</p> <code>REST_PATH</code> <code>str</code> <p>URL prefix at which the REST API is mounted.</p> <code>DATABASE_URL</code> <code>str</code> <p>SQLAlchemy-compatible database connection URL.</p>"},{"location":"api/","title":"API Reference","text":"<p>The <code>qupboard_graphql.api</code> package contains all HTTP-facing code: the FastAPI application factory, the REST router, the GraphQL router and type declarations, and the root/health-check routes.</p>"},{"location":"api/app/","title":"Application Factory","text":""},{"location":"api/app/#qupboard_graphql.api.app","title":"<code>qupboard_graphql.api.app</code>","text":"<p>FastAPI application factory for qupboard_graphql.</p> <p>Assembles the REST router, GraphQL router, and root/health-check router into a single FastAPI application.  A custom OpenAPI schema is injected so that the GraphQL endpoint appears correctly in the generated docs.</p>"},{"location":"api/app/#qupboard_graphql.api.app.get_app","title":"<code>get_app()</code>","text":"<p>Create and configure the FastAPI application.</p> <p>Registers all routers (root, REST, GraphQL) and installs the custom OpenAPI schema builder.</p> <p>Returns:</p> Type Description <code>FastAPI</code> <p>A fully-configured :class:<code>fastapi.FastAPI</code> application instance.</p> Source code in <code>src/qupboard_graphql/api/app.py</code> <pre><code>def get_app() -&gt; FastAPI:\n    \"\"\"Create and configure the FastAPI application.\n\n    Registers all routers (root, REST, GraphQL) and installs the custom\n    OpenAPI schema builder.\n\n    Returns:\n        A fully-configured :class:`fastapi.FastAPI` application instance.\n    \"\"\"\n    app = FastAPI()\n    app.include_router(root_router, prefix=\"\")\n    app.include_router(rest_router, prefix=settings.REST_PATH)\n    app.include_router(graphql_router, prefix=settings.GRAPHQL_PATH)\n    app.openapi = lambda: _custom_openapi(app)\n    return app\n</code></pre>"},{"location":"api/graphql/","title":"GraphQL Resolvers &amp; Schema","text":""},{"location":"api/graphql/#qupboard_graphql.api.graphql","title":"<code>qupboard_graphql.api.graphql</code>","text":"<p>GraphQL query resolvers, schema, and FastAPI router.</p> <p>Strawberry type declarations live in graphql_types.py.</p>"},{"location":"api/graphql/#qupboard_graphql.api.graphql.Query","title":"<code>Query</code>","text":"<p>Root GraphQL query type exposing hardware-model calibration data.</p>"},{"location":"api/graphql/#qupboard_graphql.api.graphql.Query.get_calibration","title":"<code>get_calibration(info, id)</code>","text":"<p>Retrieve a single hardware model by its UUID.</p> <p>Parameters:</p> Name Type Description Default <code>info</code> <code>Info</code> <p>Strawberry resolver context carrying the database session.</p> required <code>id</code> <code>UUID</code> <p>UUID of the hardware model to retrieve.</p> required <p>Returns:</p> Type Description <code>HardwareModel | None</code> <p>The matching :class:<code>HardwareModel</code> GraphQL object, or <code>None</code></p> <code>HardwareModel | None</code> <p>if no record with the given UUID exists.</p> Source code in <code>src/qupboard_graphql/api/graphql.py</code> <pre><code>@strawberry.field\ndef get_calibration(self, info: strawberry.types.Info, id: UUID) -&gt; HardwareModel | None:\n    \"\"\"Retrieve a single hardware model by its UUID.\n\n    Args:\n        info: Strawberry resolver context carrying the database session.\n        id: UUID of the hardware model to retrieve.\n\n    Returns:\n        The matching :class:`HardwareModel` GraphQL object, or ``None``\n        if no record with the given UUID exists.\n    \"\"\"\n    db = info.context[\"db\"]\n    return HardwareModelORM.get_by_uuid(db, id)\n</code></pre>"},{"location":"api/graphql/#qupboard_graphql.api.graphql.Query.get_all_hardware_model_ids","title":"<code>get_all_hardware_model_ids(info)</code>","text":"<p>Return the UUIDs of all hardware models stored in the database.</p> <p>Parameters:</p> Name Type Description Default <code>info</code> <code>Info</code> <p>Strawberry resolver context carrying the database session.</p> required <p>Returns:</p> Type Description <code>list[UUID]</code> <p>A list of UUIDs, one per stored hardware model.</p> Source code in <code>src/qupboard_graphql/api/graphql.py</code> <pre><code>@strawberry.field\ndef get_all_hardware_model_ids(self, info: strawberry.types.Info) -&gt; list[UUID]:\n    \"\"\"Return the UUIDs of all hardware models stored in the database.\n\n    Args:\n        info: Strawberry resolver context carrying the database session.\n\n    Returns:\n        A list of UUIDs, one per stored hardware model.\n    \"\"\"\n    db = info.context[\"db\"]\n    return HardwareModelORM.get_all_pks(db)\n</code></pre>"},{"location":"api/graphql/#qupboard_graphql.api.graphql.Query.get_all_calibrations","title":"<code>get_all_calibrations(info, first=None, after=None, last=None, before=None)</code>","text":"<p>Return a paginated connection of all hardware models in the database.</p> <p>Supports relay-style cursor pagination via <code>first</code>/<code>after</code> (forward) and <code>last</code>/<code>before</code> (backward), with <code>pageInfo</code> and per-edge cursors, matching the connection shape used by relationship fields such as <code>qubits</code>.</p> <p>Parameters:</p> Name Type Description Default <code>info</code> <code>Info</code> <p>Strawberry resolver context carrying the database session.</p> required <code>first</code> <code>Optional[int]</code> <p>Return the first n records after <code>after</code>.</p> <code>None</code> <code>after</code> <code>Optional[str]</code> <p>Cursor from a previous <code>endCursor</code> \u2014 start after this position.</p> <code>None</code> <code>last</code> <code>Optional[int]</code> <p>Return the last n records before <code>before</code>.</p> <code>None</code> <code>before</code> <code>Optional[str]</code> <p>Cursor from a previous <code>startCursor</code> \u2014 end before this position.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>HardwareModelConnection</code> <p>class:<code>HardwareModelConnection</code> containing <code>edges</code>, per-edge</p> <code>HardwareModelConnection</code> <p><code>cursor</code> values, and a <code>pageInfo</code> block.</p> Source code in <code>src/qupboard_graphql/api/graphql.py</code> <pre><code>@strawberry.field\ndef get_all_calibrations(\n    self,\n    info: strawberry.types.Info,\n    first: Optional[int] = None,\n    after: Optional[str] = None,\n    last: Optional[int] = None,\n    before: Optional[str] = None,\n) -&gt; HardwareModelConnection:\n    \"\"\"Return a paginated connection of all hardware models in the database.\n\n    Supports relay-style cursor pagination via ``first``/``after`` (forward)\n    and ``last``/``before`` (backward), with ``pageInfo`` and per-edge cursors,\n    matching the connection shape used by relationship fields such as ``qubits``.\n\n    Args:\n        info: Strawberry resolver context carrying the database session.\n        first: Return the first *n* records after ``after``.\n        after: Cursor from a previous ``endCursor`` \u2014 start after this position.\n        last: Return the last *n* records before ``before``.\n        before: Cursor from a previous ``startCursor`` \u2014 end before this position.\n\n    Returns:\n        A :class:`HardwareModelConnection` containing ``edges``, per-edge\n        ``cursor`` values, and a ``pageInfo`` block.\n    \"\"\"\n    db = info.context[\"db\"]\n    # Pass the Query object \u2014 resolve_connection slices it as nodes[start:overfetch],\n    # which SQLAlchemy translates to LIMIT/OFFSET SQL rather than fetching all rows.\n    nodes = db.query(HardwareModelORM)\n    return HardwareModelConnection.resolve_connection(\n        nodes,\n        info=info,\n        first=first,\n        after=after,\n        last=last,\n        before=before,\n    )\n</code></pre>"},{"location":"api/graphql/#qupboard_graphql.api.graphql.get_db_context","title":"<code>get_db_context(db=Depends(get_db))</code>  <code>async</code>","text":"<p>FastAPI dependency that builds the Strawberry request context.</p> <p>Provides a SQLAlchemy session and a :class:<code>StrawberrySQLAlchemyLoader</code> to all GraphQL resolvers via <code>info.context</code>.</p> <p>Parameters:</p> Name Type Description Default <code>db</code> <code>Session</code> <p>An active SQLAlchemy session injected by :func:<code>get_db</code>.</p> <code>Depends(get_db)</code> <p>Returns:</p> Type Description <code>dict</code> <p>A dictionary with <code>\"db\"</code> and <code>\"sqlalchemy_loader\"</code> keys.</p> Source code in <code>src/qupboard_graphql/api/graphql.py</code> <pre><code>async def get_db_context(db: Session = Depends(get_db)) -&gt; dict:\n    \"\"\"FastAPI dependency that builds the Strawberry request context.\n\n    Provides a SQLAlchemy session and a :class:`StrawberrySQLAlchemyLoader`\n    to all GraphQL resolvers via ``info.context``.\n\n    Args:\n        db: An active SQLAlchemy session injected by :func:`get_db`.\n\n    Returns:\n        A dictionary with ``\"db\"`` and ``\"sqlalchemy_loader\"`` keys.\n    \"\"\"\n    return {\"db\": db, \"sqlalchemy_loader\": StrawberrySQLAlchemyLoader(bind=db)}\n</code></pre>"},{"location":"api/graphql_types/","title":"GraphQL Types","text":""},{"location":"api/graphql_types/#qupboard_graphql.api.graphql_types","title":"<code>qupboard_graphql.api.graphql_types</code>","text":"<p>Strawberry GraphQL type declarations, generated from the SQLAlchemy ORM models via strawberry-sqlalchemy-mapper.</p> <p>All @mapper.type classes live here so that graphql.py can stay focused on query resolvers, schema construction, and router wiring.</p>"},{"location":"api/graphql_types/#qupboard_graphql.api.graphql_types.PhysicalChannel","title":"<code>PhysicalChannel</code>","text":"<p>GraphQL type for a physical channel (qubit or resonator).</p> <p>The <code>qubit</code> and <code>resonator</code> back-references are excluded to avoid circular type definitions.</p>"},{"location":"api/graphql_types/#qupboard_graphql.api.graphql_types.CalibratablePulse","title":"<code>CalibratablePulse</code>","text":"<p>GraphQL type for a calibratable pulse waveform.</p>"},{"location":"api/graphql_types/#qupboard_graphql.api.graphql_types.PulseChannel","title":"<code>PulseChannel</code>","text":"<p>GraphQL type for a pulse channel (drive, measure, acquire, reset, etc.).</p>"},{"location":"api/graphql_types/#qupboard_graphql.api.graphql_types.CrossResonanceChannel","title":"<code>CrossResonanceChannel</code>","text":"<p>GraphQL type for a cross-resonance or cross-resonance-cancellation channel.</p> <p>The <code>qubit</code> back-reference is excluded to avoid circular type definitions.</p>"},{"location":"api/graphql_types/#qupboard_graphql.api.graphql_types.ZxPi4Comp","title":"<code>ZxPi4Comp</code>","text":"<p>GraphQL type for a ZX-\u03c0/4 compensation element.</p> <p>The <code>qubit</code> back-reference is excluded to avoid circular type definitions.</p>"},{"location":"api/graphql_types/#qupboard_graphql.api.graphql_types.Resonator","title":"<code>Resonator</code>","text":"<p>GraphQL type for a resonator coupled to a qubit.</p> <p>The <code>qubit</code> back-reference is excluded to avoid circular type definitions.</p>"},{"location":"api/graphql_types/#qupboard_graphql.api.graphql_types.Qubit","title":"<code>Qubit</code>","text":"<p>GraphQL type for a qubit and its associated calibration data.</p> <p>The <code>hardware_model</code> back-reference is excluded to avoid circular type definitions.  <code>mean_z_map_args</code> is resolved from its JSON representation via a custom field resolver.</p>"},{"location":"api/graphql_types/#qupboard_graphql.api.graphql_types.Qubit.mean_z_map_args","title":"<code>mean_z_map_args()</code>","text":"<p>Deserialise and return the mean-Z map arguments.</p> <p>Returns:</p> Type Description <code>list[float]</code> <p>A list of floats decoded from the JSON-encoded <code>mean_z_map_args</code></p> <code>list[float]</code> <p>column stored on the underlying ORM row.</p> Source code in <code>src/qupboard_graphql/api/graphql_types.py</code> <pre><code>@strawberry.field\ndef mean_z_map_args(self) -&gt; list[float]:\n    \"\"\"Deserialise and return the mean-Z map arguments.\n\n    Returns:\n        A list of floats decoded from the JSON-encoded ``mean_z_map_args``\n        column stored on the underlying ORM row.\n    \"\"\"\n    return json.loads(self.mean_z_map_args)  # type: ignore[attr-defined]\n</code></pre>"},{"location":"api/graphql_types/#qupboard_graphql.api.graphql_types.HardwareModel","title":"<code>HardwareModel</code>","text":"<p>GraphQL type for the top-level hardware model calibration record.</p>"},{"location":"api/rest/","title":"REST API","text":""},{"location":"api/rest/#qupboard_graphql.api.rest","title":"<code>qupboard_graphql.api.rest</code>","text":"<p>REST API routes for managing hardware-model calibration records.</p> <p>Exposes CRUD-style endpoints under the prefix defined by :data:<code>~qupboard_graphql.config.Settings.REST_PATH</code> (default <code>/rest</code>).</p>"},{"location":"api/rest/#qupboard_graphql.api.rest.get_all_logical_hardware_ids","title":"<code>get_all_logical_hardware_ids(db=Depends(get_db))</code>  <code>async</code>","text":"<p>Return the UUIDs of all hardware models currently stored in the database.</p> <p>Parameters:</p> Name Type Description Default <code>db</code> <code>Session</code> <p>An active SQLAlchemy session injected by :func:<code>~qupboard_graphql.db.session.get_db</code>.</p> <code>Depends(get_db)</code> <p>Returns:</p> Type Description <code>list[UUID]</code> <p>A list of UUIDs, one per stored hardware model.</p> Source code in <code>src/qupboard_graphql/api/rest.py</code> <pre><code>@rest_router.get(\n    \"/logical-hardware\",\n    summary=\"List all hardware model IDs\",\n    response_description=\"A list of UUIDs for every stored hardware model\",\n)\nasync def get_all_logical_hardware_ids(\n    db: Session = Depends(get_db),\n) -&gt; list[UUID]:\n    \"\"\"Return the UUIDs of all hardware models currently stored in the database.\n\n    Args:\n        db: An active SQLAlchemy session injected by :func:`~qupboard_graphql.db.session.get_db`.\n\n    Returns:\n        A list of UUIDs, one per stored hardware model.\n    \"\"\"\n    return HardwareModelORM.get_all_pks(db)\n</code></pre>"},{"location":"api/rest/#qupboard_graphql.api.rest.get_logical_hardware","title":"<code>get_logical_hardware(uuid, db=Depends(get_db))</code>  <code>async</code>","text":"<p>Retrieve a single hardware model by its UUID.</p> <p>Parameters:</p> Name Type Description Default <code>uuid</code> <code>UUID</code> <p>UUID of the hardware model to retrieve.</p> required <code>db</code> <code>Session</code> <p>An active SQLAlchemy session injected by :func:<code>~qupboard_graphql.db.session.get_db</code>.</p> <code>Depends(get_db)</code> <p>Returns:</p> Name Type Description <code>The</code> <code>HardwareModel</code> <p>class:<code>~qupboard_graphql.schemas.hardware_model.HardwareModel</code> matching uuid.</p> <p>Raises:</p> Type Description <code>HTTPException</code> <p>404 if no hardware model with the given UUID exists.</p> Source code in <code>src/qupboard_graphql/api/rest.py</code> <pre><code>@rest_router.get(\n    \"/logical-hardware/{uuid}\",\n    summary=\"Get a hardware model by ID\",\n    response_description=\"The hardware model matching the given UUID\",\n    responses={404: {\"description\": \"Hardware model not found\"}},\n)\nasync def get_logical_hardware(\n    uuid: UUID,\n    db: Session = Depends(get_db),\n) -&gt; HardwareModel:\n    \"\"\"Retrieve a single hardware model by its UUID.\n\n    Args:\n        uuid: UUID of the hardware model to retrieve.\n        db: An active SQLAlchemy session injected by :func:`~qupboard_graphql.db.session.get_db`.\n\n    Returns:\n        The :class:`~qupboard_graphql.schemas.hardware_model.HardwareModel` matching *uuid*.\n\n    Raises:\n        HTTPException: 404 if no hardware model with the given UUID exists.\n    \"\"\"\n    orm_obj = HardwareModelORM.get_by_uuid(db, uuid)\n    if orm_obj is None:\n        raise HTTPException(status_code=404, detail=f\"HardwareModel {uuid} not found\")\n    return hardware_model_from_orm(orm_obj)\n</code></pre>"},{"location":"api/rest/#qupboard_graphql.api.rest.create_logical_hardware","title":"<code>create_logical_hardware(model, db=Depends(get_db))</code>  <code>async</code>","text":"<p>Persist a new hardware model supplied as a JSON body.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>HardwareModel</code> <p>A validated :class:<code>~qupboard_graphql.schemas.hardware_model.HardwareModel</code> instance parsed from the request body.</p> required <code>db</code> <code>Session</code> <p>An active SQLAlchemy session injected by :func:<code>~qupboard_graphql.db.session.get_db</code>.</p> <code>Depends(get_db)</code> <p>Returns:</p> Type Description <code>UUID</code> <p>The UUID assigned to the newly created hardware model record.</p> <p>Raises:</p> Type Description <code>HTTPException</code> <p>409 if a hardware model with the same unique identifier already exists.</p> Source code in <code>src/qupboard_graphql/api/rest.py</code> <pre><code>@rest_router.post(\n    \"/logical-hardware\",\n    status_code=201,\n    summary=\"Create a hardware model\",\n    response_description=\"The UUID assigned to the newly created hardware model\",\n    responses={\n        409: {\"description\": \"A hardware model with the same unique identifier already exists\"},\n        415: {\"description\": \"Unsupported file type \u2014 only application/json is accepted\"},\n        422: {\"description\": \"File content could not be parsed as a valid hardware model\"},\n    },\n)\nasync def create_logical_hardware(\n    model: HardwareModel,\n    db: Session = Depends(get_db),\n) -&gt; UUID:\n    \"\"\"Persist a new hardware model supplied as a JSON body.\n\n    Args:\n        model: A validated :class:`~qupboard_graphql.schemas.hardware_model.HardwareModel`\n            instance parsed from the request body.\n        db: An active SQLAlchemy session injected by :func:`~qupboard_graphql.db.session.get_db`.\n\n    Returns:\n        The UUID assigned to the newly created hardware model record.\n\n    Raises:\n        HTTPException: 409 if a hardware model with the same unique identifier\n            already exists.\n    \"\"\"\n    orm_obj = hardware_model_to_orm(model)\n    db.add(orm_obj)\n    try:\n        db.commit()\n    except IntegrityError:\n        db.rollback()\n        raise HTTPException(\n            status_code=409,\n            detail=\"A hardware model with the same unique identifier already exists.\",\n        )\n    return orm_obj.id\n</code></pre>"},{"location":"api/rest/#qupboard_graphql.api.rest.upload_logical_hardware","title":"<code>upload_logical_hardware(file, db=Depends(get_db))</code>  <code>async</code>","text":"<p>Upload a JSON file containing a hardware model and persist it.</p> <p>Parameters:</p> Name Type Description Default <code>file</code> <code>UploadFile</code> <p>A JSON file whose content conforms to the :class:<code>~qupboard_graphql.schemas.hardware_model.HardwareModel</code> schema.</p> required <code>db</code> <code>Session</code> <p>An active SQLAlchemy session injected by :func:<code>~qupboard_graphql.db.session.get_db</code>.</p> <code>Depends(get_db)</code> <p>Returns:</p> Type Description <code>UUID</code> <p>The UUID assigned to the newly created hardware model record.</p> <p>Raises:</p> Type Description <code>HTTPException</code> <p>415 if the file's content type is not <code>application/json</code> or <code>text/plain</code>.</p> <code>HTTPException</code> <p>422 if the file content cannot be parsed as a valid <code>HardwareModel</code>.</p> <code>HTTPException</code> <p>409 if a hardware model with the same unique identifier already exists.</p> Source code in <code>src/qupboard_graphql/api/rest.py</code> <pre><code>@rest_router.post(\n    \"/logical-hardware/upload\",\n    status_code=201,\n    summary=\"Upload a hardware model from a file\",\n    response_description=\"The UUID assigned to the newly created hardware model\",\n    responses={\n        409: {\"description\": \"A hardware model with the same unique identifier already exists\"},\n        415: {\"description\": \"Unsupported file type \u2014 only application/json is accepted\"},\n        422: {\"description\": \"File content could not be parsed as a valid hardware model\"},\n    },\n)\nasync def upload_logical_hardware(\n    file: UploadFile,\n    db: Session = Depends(get_db),\n) -&gt; UUID:\n    \"\"\"Upload a JSON file containing a hardware model and persist it.\n\n    Args:\n        file: A JSON file whose content conforms to the\n            :class:`~qupboard_graphql.schemas.hardware_model.HardwareModel` schema.\n        db: An active SQLAlchemy session injected by :func:`~qupboard_graphql.db.session.get_db`.\n\n    Returns:\n        The UUID assigned to the newly created hardware model record.\n\n    Raises:\n        HTTPException: 415 if the file's content type is not ``application/json``\n            or ``text/plain``.\n        HTTPException: 422 if the file content cannot be parsed as a valid\n            ``HardwareModel``.\n        HTTPException: 409 if a hardware model with the same unique identifier\n            already exists.\n    \"\"\"\n    if file.content_type not in (\"application/json\", \"text/plain\"):\n        raise HTTPException(\n            status_code=415,\n            detail=f\"Unsupported file type '{file.content_type}'. Expected application/json.\",\n        )\n    raw = await file.read()\n    try:\n        model = HardwareModel.model_validate_json(raw)\n    except Exception as exc:\n        raise HTTPException(status_code=422, detail=f\"Invalid hardware model file: {exc}\") from exc\n    orm_obj = hardware_model_to_orm(model)\n    db.add(orm_obj)\n    try:\n        db.commit()\n    except IntegrityError:\n        db.rollback()\n        raise HTTPException(\n            status_code=409,\n            detail=\"A hardware model with the same unique identifier already exists.\",\n        )\n    return orm_obj.id\n</code></pre>"},{"location":"api/root/","title":"Root &amp; Health Check","text":""},{"location":"api/root/#qupboard_graphql.api.root","title":"<code>qupboard_graphql.api.root</code>","text":"<p>Root and health-check routes for the qupboard_graphql service.</p> <p>Provides a <code>/healthcheck</code> endpoint for liveness probes and a <code>/</code> redirect to the interactive API documentation.</p>"},{"location":"api/root/#qupboard_graphql.api.root.healthcheck","title":"<code>healthcheck()</code>  <code>async</code>","text":"<p>Return a simple OK response to confirm the service is running.</p> Source code in <code>src/qupboard_graphql/api/root.py</code> <pre><code>@root_router.get(\"/healthcheck\", tags=[\"Health\"], summary=\"Health check\")\nasync def healthcheck() -&gt; Response:\n    \"\"\"Return a simple OK response to confirm the service is running.\"\"\"\n    return Response(\"OK\")\n</code></pre>"},{"location":"api/root/#qupboard_graphql.api.root.root","title":"<code>root()</code>  <code>async</code>","text":"<p>Redirect the root path to the auto-generated API documentation.</p> Source code in <code>src/qupboard_graphql/api/root.py</code> <pre><code>@root_router.get(\"/\", include_in_schema=False)\nasync def root():\n    \"\"\"Redirect the root path to the auto-generated API documentation.\"\"\"\n    return RedirectResponse(\"/docs\")\n</code></pre>"},{"location":"db/","title":"Database","text":"<p>The <code>qupboard_graphql.db</code> package contains everything that touches the database: the SQLAlchemy declarative base, ORM models, session and engine management, the repository mixin, and the bidirectional Pydantic \u2194 ORM mapper utilities.</p>"},{"location":"db/mapper_from_orm/","title":"ORM \u2192 Pydantic Mapper","text":""},{"location":"db/mapper_from_orm/#qupboard_graphql.db.mapper_from_orm","title":"<code>qupboard_graphql.db.mapper_from_orm</code>","text":"<p>Utilities for converting SQLAlchemy ORM instances \u2192 Pydantic HardwareModel.</p> <p>The public API is :func:<code>hardware_model_from_orm</code>.</p> <p>All private helpers (prefixed <code>_</code>) are internal implementation details.</p>"},{"location":"db/mapper_from_orm/#qupboard_graphql.db.mapper_from_orm.hardware_model_from_orm","title":"<code>hardware_model_from_orm(orm)</code>","text":"<p>Convert a fully-loaded ORM HardwareModelORM back into a Pydantic HardwareModel.</p> <p>Parameters:</p> Name Type Description Default <code>orm</code> <code>HardwareModelORM</code> <p>A fully-loaded :class:<code>~qupboard_graphql.db.models.HardwareModelORM</code> instance (all relationships must be accessible).</p> required <p>Returns:</p> Type Description <code>HardwareModel</code> <p>The reconstructed</p> <code>HardwareModel</code> <p>class:<code>~qupboard_graphql.schemas.hardware_model.HardwareModel</code></p> <code>HardwareModel</code> <p>instance.</p> Source code in <code>src/qupboard_graphql/db/mapper_from_orm.py</code> <pre><code>def hardware_model_from_orm(orm: HardwareModelORM) -&gt; HardwareModel:\n    \"\"\"Convert a fully-loaded ORM HardwareModelORM back into a Pydantic HardwareModel.\n\n    Args:\n        orm: A fully-loaded\n            :class:`~qupboard_graphql.db.models.HardwareModelORM` instance\n            (all relationships must be accessible).\n\n    Returns:\n        The reconstructed\n        :class:`~qupboard_graphql.schemas.hardware_model.HardwareModel`\n        instance.\n    \"\"\"\n    qubits = dict(_qubit_from_orm(q) for q in orm.qubits)\n    return HardwareModel(\n        version=orm.version,\n        calibration_id=orm.calibration_id,\n        logical_connectivity=json.loads(orm.logical_connectivity),\n        qubits=qubits,\n    )\n</code></pre>"},{"location":"db/mapper_to_orm/","title":"Pydantic \u2192 ORM Mapper","text":""},{"location":"db/mapper_to_orm/#qupboard_graphql.db.mapper_to_orm","title":"<code>qupboard_graphql.db.mapper_to_orm</code>","text":"<p>Utilities for converting Pydantic HardwareModel \u2192 SQLAlchemy ORM instances.</p> <p>The public API is :func:<code>hardware_model_to_orm</code>.</p> <p>All private helpers (prefixed <code>_</code>) are internal implementation details.</p>"},{"location":"db/mapper_to_orm/#qupboard_graphql.db.mapper_to_orm.hardware_model_to_orm","title":"<code>hardware_model_to_orm(model)</code>","text":"<p>Convert a validated Pydantic HardwareModel into a fully-populated ORM tree.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>HardwareModel</code> <p>A validated :class:<code>~qupboard_graphql.schemas.hardware_model.HardwareModel</code> instance.</p> required <p>Returns:</p> Type Description <code>HardwareModelORM</code> <p>A new, unsaved</p> <code>HardwareModelORM</code> <p>class:<code>~qupboard_graphql.db.models.HardwareModelORM</code> instance with</p> <code>HardwareModelORM</code> <p>all child rows attached and ready for <code>session.add()</code> followed by</p> <code>HardwareModelORM</code> <p><code>session.commit()</code>.</p> Source code in <code>src/qupboard_graphql/db/mapper_to_orm.py</code> <pre><code>def hardware_model_to_orm(model: HardwareModel) -&gt; HardwareModelORM:\n    \"\"\"Convert a validated Pydantic HardwareModel into a fully-populated ORM tree.\n\n    Args:\n        model: A validated\n            :class:`~qupboard_graphql.schemas.hardware_model.HardwareModel`\n            instance.\n\n    Returns:\n        A new, unsaved\n        :class:`~qupboard_graphql.db.models.HardwareModelORM` instance with\n        all child rows attached and ready for ``session.add()`` followed by\n        ``session.commit()``.\n    \"\"\"\n    qubit_orms = [_qubit_orm(key, qubit) for key, qubit in model.qubits.items()]\n    return HardwareModelORM(\n        version=model.version,\n        calibration_id=model.calibration_id,\n        logical_connectivity=json.dumps(model.logical_connectivity),\n        qubits=qubit_orms,\n    )\n</code></pre>"},{"location":"db/models/","title":"ORM Models","text":""},{"location":"db/models/#qupboard_graphql.db.models","title":"<code>qupboard_graphql.db.models</code>","text":"<p>SQLAlchemy ORM models mirroring the Pydantic HardwareModel schema.</p>"},{"location":"db/models/#qupboard_graphql.db.models--table-hierarchy","title":"Table hierarchy","text":"<p>hardware_models   \u2514\u2500 qubits  (FK \u2192 hardware_models)        \u251c\u2500 physical_channels        (FK \u2192 qubits OR resonators, channel_kind = 'qubit' | 'resonator')        \u2502    baseband and IQ-bias columns inlined here        \u251c\u2500 pulse_channels           (FK \u2192 qubits OR resonators, channel_role discriminator)        \u2502    channel_role values:        \u2502      'drive'            \u2013 qubit drive channel        \u2502      'second_state'     \u2013 second-state pulse channel (ss_active, ss_delay)        \u2502      'freq_shift'       \u2013 freq-shift pulse channel   (fs_active, fs_amp, fs_phase)        \u2502      'measure'          \u2013 resonator measure channel        \u2502      'acquire'          \u2013 resonator acquire channel  (acq_delay/width/sync/use_weights)        \u2502      'reset_qubit'      \u2013 qubit reset channel        (reset_delay)        \u2502      'reset_resonator'  \u2013 resonator reset channel    (reset_delay)        \u2502    \u2514\u2500 calibratable_pulses (owner_uuid \u2192 pulse_channels.id, pulse_role discriminator)        \u251c\u2500 cross_resonance_channels (FK \u2192 qubits, role = 'cr' | 'crc')        \u2502    \u2514\u2500 calibratable_pulses (pulse_role = 'cr')        \u251c\u2500 zx_pi_4_comps            (FK \u2192 qubits, one per CR pair)        \u2502    \u2514\u2500 calibratable_pulses (pulse_role = 'zx_precomp' | 'zx_postcomp', nullable)        \u2502    phase_comp_x_pi_2 inlined on qubits        \u2514\u2500 resonators               (FK \u2192 qubits, one-to-one)             \u251c\u2500 physical_channels   (FK \u2192 resonators, channel_kind = 'resonator')             \u2514\u2500 pulse_channels      (FK \u2192 resonators, channel_role = 'measure' | 'acquire' | 'reset_resonator')</p>"},{"location":"db/models/#qupboard_graphql.db.models.HardwareModelORM","title":"<code>HardwareModelORM</code>","text":"<p>               Bases: <code>RepositoryMixin</code>, <code>Base</code></p> <p>Top-level hardware model record containing version and qubit calibration data.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>Mapped[UUID]</code> <p>Auto-generated UUID primary key.</p> <code>version</code> <code>Mapped[str]</code> <p>Schema or calibration version string.</p> <code>calibration_id</code> <code>Mapped[str]</code> <p>Identifier for the calibration run.</p> <code>logical_connectivity</code> <code>Mapped[str]</code> <p>JSON-encoded mapping of qubit label to list of neighbour indices.</p> <code>qubits</code> <code>Mapped[list[QubitORM]]</code> <p>One-to-many collection of associated :class:<code>QubitORM</code> rows.</p>"},{"location":"db/models/#qupboard_graphql.db.models.PhysicalChannelORM","title":"<code>PhysicalChannelORM</code>","text":"<p>               Bases: <code>Base</code></p> <p>Unified physical channel row covering both qubit and resonator channels.</p> <p>A single table (<code>physical_channels</code>) stores physical channels for both qubits and resonators, discriminated by channel_kind.  The inlined <code>BaseBand</code> and <code>IQVoltageBias</code> Pydantic fields are flattened into columns here rather than in separate tables.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <p>UUID primary key (shared with the Pydantic schema's UUID).</p> <code>channel_kind</code> <code>Mapped[str]</code> <p><code>\"qubit\"</code> or <code>\"resonator\"</code> \u2014 identifies the owner type.</p> <code>name_index</code> <code>Mapped[int]</code> <p>Hardware name index used to address the physical port.</p> <code>block_size</code> <code>Mapped[int]</code> <p>Waveform block size in samples.</p> <code>default_amplitude</code> <code>Mapped[int]</code> <p>Default output amplitude in hardware units.</p> <code>switch_box</code> <code>Mapped[str]</code> <p>Switch-box identifier string.</p> <code>swap_readout_iq</code> <code>Mapped[bool]</code> <p>Whether to swap I and Q for readout (resonator only).</p> <code>baseband_uuid</code> <code>Mapped[UUID]</code> <p>UUID of the inlined baseband oscillator.</p> <code>baseband_frequency</code> <code>Mapped[float | None]</code> <p>Baseband carrier frequency in Hz.</p> <code>baseband_if_frequency</code> <code>Mapped[float | None]</code> <p>Intermediate frequency in Hz.</p> <code>iq_bias</code> <code>Mapped[str]</code> <p>Serialised IQ voltage bias correction string.</p> <code>qubit_uuid</code> <code>Mapped[UUID | None]</code> <p>FK to :class:<code>QubitORM</code> (set when <code>channel_kind = \"qubit\"</code>).</p> <code>resonator_uuid</code> <code>Mapped[UUID | None]</code> <p>FK to :class:<code>ResonatorORM</code> (set when <code>channel_kind = \"resonator\"</code>).</p> <code>qubit</code> <code>Mapped[QubitORM | None]</code> <p>Relationship to the owning :class:<code>QubitORM</code>.</p> <code>resonator</code> <code>Mapped[ResonatorORM | None]</code> <p>Relationship to the owning :class:<code>ResonatorORM</code>.</p>"},{"location":"db/models/#qupboard_graphql.db.models.CalibratablePulseORM","title":"<code>CalibratablePulseORM</code>","text":"<p>               Bases: <code>Base</code></p> <p>A single calibratable waveform pulse stored in the <code>calibratable_pulses</code> table.</p> <p>Pulses are owned by a channel row identified by owner_uuid and are further distinguished by the pulse_role discriminator column.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>Mapped[UUID]</code> <p>Auto-increment integer primary key.</p> <code>waveform_type</code> <code>Mapped[str]</code> <p>Waveform shape identifier (e.g. <code>\"gaussian\"</code>).</p> <code>width</code> <code>Mapped[float]</code> <p>Pulse duration in seconds.</p> <code>amp</code> <code>Mapped[float]</code> <p>Pulse amplitude.</p> <code>phase</code> <code>Mapped[float]</code> <p>Pulse phase in radians.</p> <code>drag</code> <code>Mapped[float]</code> <p>DRAG correction coefficient.</p> <code>rise</code> <code>Mapped[float]</code> <p>Rise-time parameter (fraction of width).</p> <code>amp_setup</code> <code>Mapped[float]</code> <p>Setup amplitude.</p> <code>std_dev</code> <code>Mapped[float]</code> <p>Gaussian standard deviation.</p> <code>owner_uuid</code> <code>Mapped[UUID]</code> <p>UUID of the owning channel row.</p> <code>pulse_role</code> <code>Mapped[str]</code> <p>Discriminator string identifying which relationship slot this pulse fills (e.g. <code>\"drive\"</code>, <code>\"drive_x_pi\"</code>, <code>\"cr\"</code>).</p>"},{"location":"db/models/#qupboard_graphql.db.models.PulseChannelORM","title":"<code>PulseChannelORM</code>","text":"<p>               Bases: <code>Base</code></p> <p>Unified pulse channel row covering all channel roles.</p> <p>A single table (<code>pulse_channels</code>) stores every role of pulse channel: drive, second_state, freq_shift, measure, acquire, reset_qubit, and reset_resonator.  Role-specific columns are <code>NULL</code> for rows of a different role.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <p>UUID primary key.</p> <code>channel_role</code> <code>Mapped[str]</code> <p>Role discriminator (<code>\"drive\"</code>, <code>\"second_state\"</code>, <code>\"freq_shift\"</code>, <code>\"measure\"</code>, <code>\"acquire\"</code>, <code>\"reset_qubit\"</code>, <code>\"reset_resonator\"</code>).</p> <code>frequency</code> <code>Mapped[float | None]</code> <p>Carrier frequency in Hz (<code>None</code> encodes NaN).</p> <code>imbalance</code> <code>Mapped[float]</code> <p>IQ imbalance correction factor.</p> <code>phase_iq_offset</code> <code>Mapped[float]</code> <p>IQ phase offset in radians.</p> <code>scale_real</code> <code>Mapped[float]</code> <p>Real part of the complex scale factor.</p> <code>scale_imag</code> <code>Mapped[float]</code> <p>Imaginary part of the complex scale factor.</p> <code>ss_active</code> <code>Mapped[bool | None]</code> <p>(second_state only) Whether second-state driving is active.</p> <code>ss_delay</code> <code>Mapped[float | None]</code> <p>(second_state only) Second-state delay in seconds.</p> <code>fs_active</code> <code>Mapped[bool | None]</code> <p>(freq_shift only) Whether frequency shifting is active.</p> <code>fs_amp</code> <code>Mapped[float | None]</code> <p>(freq_shift only) Frequency-shift amplitude.</p> <code>fs_phase</code> <code>Mapped[float | None]</code> <p>(freq_shift only) Frequency-shift phase.</p> <code>acq_delay</code> <code>Mapped[float | None]</code> <p>(acquire only) Acquisition delay in seconds.</p> <code>acq_width</code> <code>Mapped[float | None]</code> <p>(acquire only) Acquisition window width in seconds.</p> <code>acq_sync</code> <code>Mapped[bool | None]</code> <p>(acquire only) Whether to sync the acquisition.</p> <code>acq_use_weights</code> <code>Mapped[bool | None]</code> <p>(acquire only) Whether to use integration weights.</p> <code>reset_delay</code> <code>Mapped[float | None]</code> <p>(reset_qubit/reset_resonator only) Reset delay in seconds.</p> <code>qubit_uuid</code> <code>Mapped[UUID | None]</code> <p>FK to :class:<code>QubitORM</code> (set for qubit-owned channels).</p> <code>resonator_uuid</code> <code>Mapped[UUID | None]</code> <p>FK to :class:<code>ResonatorORM</code> (set for resonator channels).</p> <code>pulse</code> <code>Mapped[CalibratablePulseORM | None]</code> <p>Primary pulse waveform (most roles).</p> <code>pulse_x_pi</code> <code>Mapped[CalibratablePulseORM | None]</code> <p>X-\u03c0 pulse waveform (drive role only).</p>"},{"location":"db/models/#qupboard_graphql.db.models.CrossResonanceChannelORM","title":"<code>CrossResonanceChannelORM</code>","text":"<p>               Bases: <code>Base</code></p> <p>Unified cross-resonance channel row covering both CR and CRC channels.</p> <p>A single table (<code>cross_resonance_channels</code>) stores both CrossResonancePulseChannel (role <code>\"cr\"</code>) and CrossResonanceCancellationPulseChannel (role <code>\"crc\"</code>) rows. The <code>zx_pi_4_pulse</code> relationship is only populated for <code>\"cr\"</code> rows.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <p>UUID primary key.</p> <code>role</code> <code>Mapped[str]</code> <p><code>\"cr\"</code> for a cross-resonance channel or <code>\"crc\"</code> for a cross-resonance cancellation channel.</p> <code>auxiliary_qubit</code> <code>Mapped[int]</code> <p>Index of the auxiliary (target) qubit in the CR pair.</p> <code>frequency</code> <code>Mapped[float | None]</code> <p>Carrier frequency in Hz (<code>None</code> encodes NaN).</p> <code>imbalance</code> <code>Mapped[float]</code> <p>IQ imbalance correction factor.</p> <code>phase_iq_offset</code> <code>Mapped[float]</code> <p>IQ phase offset in radians.</p> <code>scale_real</code> <code>Mapped[float]</code> <p>Real part of the complex scale factor.</p> <code>scale_imag</code> <code>Mapped[float]</code> <p>Imaginary part of the complex scale factor.</p> <code>qubit_uuid</code> <code>Mapped[UUID | None]</code> <p>FK to the owning :class:<code>QubitORM</code>.</p> <code>qubit</code> <code>Mapped[QubitORM | None]</code> <p>Relationship to the owning :class:<code>QubitORM</code>.</p> <code>zx_pi_4_pulse</code> <code>Mapped[CalibratablePulseORM | None]</code> <p>ZX-\u03c0/4 calibration pulse (CR role only).</p>"},{"location":"db/models/#qupboard_graphql.db.models.ZxPi4CompORM","title":"<code>ZxPi4CompORM</code>","text":"<p>               Bases: <code>Base</code></p> <p>ZX-\u03c0/4 compensation element associated with a specific auxiliary qubit.</p> <p>Each row represents the calibration data for one CR-pair compensation, keyed by auxiliary_qubit index.  Optional pre- and post-compensation pulses are stored as related :class:<code>CalibratablePulseORM</code> rows.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <p>Auto-generated UUID primary key.</p> <code>auxiliary_qubit</code> <code>Mapped[int]</code> <p>Index of the auxiliary (control) qubit in the CR pair.</p> <code>phase_comp_target_zx_pi_4</code> <code>Mapped[float]</code> <p>Target ZX-\u03c0/4 phase compensation.</p> <code>pulse_zx_pi_4_target_rotary_amp</code> <code>Mapped[float | None]</code> <p>Optional rotary pulse amplitude.</p> <code>precomp_active</code> <code>Mapped[bool]</code> <p>Whether pre-compensation is enabled.</p> <code>postcomp_active</code> <code>Mapped[bool]</code> <p>Whether post-compensation is enabled.</p> <code>use_second_state</code> <code>Mapped[bool]</code> <p>Whether to use the second excited state.</p> <code>use_rotary</code> <code>Mapped[bool]</code> <p>Whether to use a rotary pulse.</p> <code>qubit_uuid</code> <code>Mapped[UUID | None]</code> <p>FK to the owning :class:<code>QubitORM</code>.</p> <code>qubit</code> <code>Mapped[QubitORM | None]</code> <p>Relationship to the owning :class:<code>QubitORM</code>.</p> <code>pulse_precomp</code> <code>Mapped[CalibratablePulseORM | None]</code> <p>Optional pre-compensation :class:<code>CalibratablePulseORM</code>.</p> <code>pulse_postcomp</code> <code>Mapped[CalibratablePulseORM | None]</code> <p>Optional post-compensation :class:<code>CalibratablePulseORM</code>.</p>"},{"location":"db/models/#qupboard_graphql.db.models.ResonatorORM","title":"<code>ResonatorORM</code>","text":"<p>               Bases: <code>Base</code></p> <p>Readout resonator coupled one-to-one with a :class:<code>QubitORM</code>.</p> <p>Holds the resonator's physical channel and its three pulse channels (measure, acquire, reset_resonator).</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <p>UUID primary key (shared with the Pydantic schema's UUID).</p> <code>qubit_uuid</code> <code>Mapped[UUID]</code> <p>FK to the parent :class:<code>QubitORM</code>.</p> <code>qubit</code> <code>Mapped[QubitORM]</code> <p>Relationship to the parent :class:<code>QubitORM</code>.</p> <code>physical_channel</code> <code>Mapped[PhysicalChannelORM | None]</code> <p>Single :class:<code>PhysicalChannelORM</code> with <code>channel_kind = \"resonator\"</code>.</p> <code>pulse_channels</code> <code>Mapped[list[PulseChannelORM]]</code> <p>All resonator pulse channels (measure, acquire, reset_resonator).</p>"},{"location":"db/models/#qupboard_graphql.db.models.ResonatorORM.measure_channel","title":"<code>measure_channel</code>  <code>property</code>","text":"<p>Return the measure pulse channel, or <code>None</code> if absent.</p>"},{"location":"db/models/#qupboard_graphql.db.models.ResonatorORM.acquire_channel","title":"<code>acquire_channel</code>  <code>property</code>","text":"<p>Return the acquire pulse channel, or <code>None</code> if absent.</p>"},{"location":"db/models/#qupboard_graphql.db.models.ResonatorORM.reset_resonator_channel","title":"<code>reset_resonator_channel</code>  <code>property</code>","text":"<p>Return the resonator reset pulse channel, or <code>None</code> if absent.</p>"},{"location":"db/models/#qupboard_graphql.db.models.QubitORM","title":"<code>QubitORM</code>","text":"<p>               Bases: <code>Base</code></p> <p>ORM representation of a single qubit and its full calibration payload.</p> <p>Owns a physical channel, multiple pulse channels (drive, second_state, freq_shift, reset_qubit), cross-resonance channels, ZX-\u03c0/4 compensation entries, and a one-to-one resonator.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <p>UUID primary key.</p> <code>qubit_key</code> <code>Mapped[str]</code> <p>String key used to identify the qubit within the model (e.g. <code>\"q0\"</code>).</p> <code>mean_z_map_args</code> <code>Mapped[str]</code> <p>JSON-encoded two-element list <code>[real, imag]</code> representing the mean-Z mapping arguments.</p> <code>discriminator_real</code> <code>Mapped[float]</code> <p>Real part of the state-discrimination threshold.</p> <code>discriminator_imag</code> <code>Mapped[float]</code> <p>Imaginary part of the state-discrimination threshold.</p> <code>direct_x_pi</code> <code>Mapped[bool]</code> <p>Whether to use a direct X-\u03c0 pulse.</p> <code>phase_comp_x_pi_2</code> <code>Mapped[float]</code> <p>X-\u03c0/2 phase compensation value.</p> <code>hardware_model_id</code> <code>Mapped[UUID | None]</code> <p>FK to the parent :class:<code>HardwareModelORM</code>.</p> <code>hardware_model</code> <code>Mapped[HardwareModelORM | None]</code> <p>Relationship to the parent :class:<code>HardwareModelORM</code>.</p> <code>physical_channel</code> <code>Mapped[PhysicalChannelORM | None]</code> <p>Single :class:<code>PhysicalChannelORM</code> with <code>channel_kind = \"qubit\"</code>.</p> <code>pulse_channels</code> <code>Mapped[list[PulseChannelORM]]</code> <p>All qubit-owned pulse channels.</p> <code>resonator</code> <code>Mapped[ResonatorORM | None]</code> <p>One-to-one :class:<code>ResonatorORM</code>.</p> <code>cross_resonance_channels</code> <code>Mapped[list[CrossResonanceChannelORM]]</code> <p>CR pulse channels (role <code>\"cr\"</code>).</p> <code>cross_resonance_cancellation_channels</code> <code>Mapped[list[CrossResonanceChannelORM]]</code> <p>CRC pulse channels (role <code>\"crc\"</code>).</p> <code>zx_pi_4_comps</code> <code>Mapped[list[ZxPi4CompORM]]</code> <p>ZX-\u03c0/4 compensation rows.</p>"},{"location":"db/models/#qupboard_graphql.db.models.QubitORM.drive_channel","title":"<code>drive_channel</code>  <code>property</code>","text":"<p>Return the drive pulse channel, or <code>None</code> if absent.</p>"},{"location":"db/models/#qupboard_graphql.db.models.QubitORM.second_state_channel","title":"<code>second_state_channel</code>  <code>property</code>","text":"<p>Return the second-state pulse channel, or <code>None</code> if absent.</p>"},{"location":"db/models/#qupboard_graphql.db.models.QubitORM.freq_shift_channel","title":"<code>freq_shift_channel</code>  <code>property</code>","text":"<p>Return the frequency-shift pulse channel, or <code>None</code> if absent.</p>"},{"location":"db/models/#qupboard_graphql.db.models.QubitORM.reset_qubit_channel","title":"<code>reset_qubit_channel</code>  <code>property</code>","text":"<p>Return the qubit reset pulse channel, or <code>None</code> if absent.</p>"},{"location":"db/repository/","title":"Repository Mixin","text":""},{"location":"db/repository/#qupboard_graphql.db.repository","title":"<code>qupboard_graphql.db.repository</code>","text":"<p>Repository mixin providing generic primary-key query helpers.</p> <p>Inherit alongside DeclarativeBase to give ORM models <code>get_by_uuid</code> and <code>get_all_pks</code> without cluttering the base class itself.</p>"},{"location":"db/repository/#qupboard_graphql.db.repository.RepositoryMixin","title":"<code>RepositoryMixin</code>","text":"<p>Mixin that adds common primary-key query helpers to SQLAlchemy ORM models.</p> <p>Classes that inherit from both :class:<code>RepositoryMixin</code> and SQLAlchemy's :class:<code>~sqlalchemy.orm.DeclarativeBase</code> gain :meth:<code>get_by_uuid</code> and :meth:<code>get_all_pks</code> as class-level helpers without needing a separate repository object.</p>"},{"location":"db/repository/#qupboard_graphql.db.repository.RepositoryMixin.get_by_uuid","title":"<code>get_by_uuid(session, uuid)</code>  <code>classmethod</code>","text":"<p>Return the row whose primary-key column matches uuid.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>Session</code> <p>An active SQLAlchemy session.</p> required <code>uuid</code> <code>UUID</code> <p>The primary-key value to look up.</p> required <p>Returns:</p> Type Description <code>Self | None</code> <p>The matching ORM instance, or <code>None</code> if no row is found.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the model class has no primary key defined.</p> Source code in <code>src/qupboard_graphql/db/repository.py</code> <pre><code>@classmethod\ndef get_by_uuid(cls, session: Session, uuid: UUID) -&gt; Self | None:\n    \"\"\"Return the row whose primary-key column matches *uuid*.\n\n    Args:\n        session: An active SQLAlchemy session.\n        uuid: The primary-key value to look up.\n\n    Returns:\n        The matching ORM instance, or ``None`` if no row is found.\n\n    Raises:\n        TypeError: If the model class has no primary key defined.\n    \"\"\"\n    pk_cols = inspect(cls).mapper.primary_key\n    if not pk_cols:\n        raise TypeError(f\"{cls.__name__} has no primary key defined\")\n    pk_col = pk_cols[0]\n    return session.query(cls).filter(pk_col == uuid).one_or_none()\n</code></pre>"},{"location":"db/repository/#qupboard_graphql.db.repository.RepositoryMixin.get_all_pks","title":"<code>get_all_pks(session)</code>  <code>classmethod</code>","text":"<p>Return a list of all primary key values for this model's table.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>Session</code> <p>An active SQLAlchemy session.</p> required <p>Returns:</p> Type Description <code>list</code> <p>A list containing every primary key value in the table, in</p> <code>list</code> <p>database-defined order.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the model class has no primary key defined.</p> Source code in <code>src/qupboard_graphql/db/repository.py</code> <pre><code>@classmethod\ndef get_all_pks(cls, session: Session) -&gt; list:\n    \"\"\"Return a list of all primary key values for this model's table.\n\n    Args:\n        session: An active SQLAlchemy session.\n\n    Returns:\n        A list containing every primary key value in the table, in\n        database-defined order.\n\n    Raises:\n        TypeError: If the model class has no primary key defined.\n    \"\"\"\n    pk_cols = inspect(cls).mapper.primary_key\n    if not pk_cols:\n        raise TypeError(f\"{cls.__name__} has no primary key defined\")\n    pk_col = pk_cols[0]\n    return [row[0] for row in session.query(pk_col).all()]\n</code></pre>"},{"location":"db/session/","title":"Session &amp; Engine","text":""},{"location":"db/session/#qupboard_graphql.db.session","title":"<code>qupboard_graphql.db.session</code>","text":"<p>SQLAlchemy engine and session management for qupboard_graphql.</p> <p>Provides a shared module-level :class:<code>~sqlalchemy.engine.Engine</code> and a FastAPI-compatible generator dependency that yields a per-request :class:<code>~sqlalchemy.orm.Session</code>.</p>"},{"location":"db/session/#qupboard_graphql.db.session.get_engine","title":"<code>get_engine(database_url=None)</code>","text":"<p>Create a SQLAlchemy engine with backend-specific connect args.</p> <p>Parameters:</p> Name Type Description Default <code>database_url</code> <code>str | None</code> <p>Optional SQLAlchemy URL. Defaults to <code>settings.DATABASE_URL</code>.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Engine</code> <code>Engine</code> <p>Configured SQLAlchemy engine.</p> Source code in <code>src/qupboard_graphql/db/session.py</code> <pre><code>def get_engine(database_url: str | None = None) -&gt; Engine:\n    \"\"\"Create a SQLAlchemy engine with backend-specific connect args.\n\n    Args:\n        database_url: Optional SQLAlchemy URL. Defaults to ``settings.DATABASE_URL``.\n\n    Returns:\n        Engine: Configured SQLAlchemy engine.\n    \"\"\"\n    resolved_url = database_url or settings.DATABASE_URL\n    url = make_url(resolved_url)\n    connect_args: dict[str, Any] = {}\n\n    # SQLite needs thread-check disabling for our app/test usage with FastAPI.\n    if url.get_backend_name() == \"sqlite\":\n        connect_args[\"check_same_thread\"] = False\n\n    engine = create_engine(resolved_url, connect_args=connect_args)\n\n    # Enforce FK constraints in SQLite (off by default unless pragma is set per connection).\n    if url.get_backend_name() == \"sqlite\":\n        event.listen(engine, \"connect\", _set_sqlite_pragma)\n\n    return engine\n</code></pre>"},{"location":"db/session/#qupboard_graphql.db.session.get_db","title":"<code>get_db()</code>","text":"<p>FastAPI dependency that yields a SQLAlchemy session.</p> <p>Opens a new session before each request and ensures it is closed afterwards, even if an exception is raised.</p> <p>Yields:</p> Type Description <code>Session</code> <p>An active :class:<code>~sqlalchemy.orm.Session</code> bound to the module engine.</p> Source code in <code>src/qupboard_graphql/db/session.py</code> <pre><code>def get_db() -&gt; Generator[Session, None, None]:\n    \"\"\"FastAPI dependency that yields a SQLAlchemy session.\n\n    Opens a new session before each request and ensures it is closed\n    afterwards, even if an exception is raised.\n\n    Yields:\n        An active :class:`~sqlalchemy.orm.Session` bound to the module engine.\n    \"\"\"\n    db: Session = session_factory()\n    try:\n        yield db\n    finally:\n        db.close()\n</code></pre>"},{"location":"schemas/","title":"Schemas","text":"<p>The <code>qupboard_graphql.schemas</code> package contains the Pydantic model hierarchy that defines the shape of a hardware model calibration record. These schemas are used for REST request/response validation and as the source of truth that the ORM mapping layer converts to and from.</p>"},{"location":"schemas/hardware_model/","title":"Hardware Model Schema","text":""},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model","title":"<code>qupboard_graphql.schemas.hardware_model</code>","text":"<p>Pydantic schema hierarchy for a quantum hardware model calibration record.</p> <p>These schemas are used for request/response validation in the REST API and as the source of truth for the ORM mapping layer.  The top-level entry point is :class:<code>HardwareModel</code>.</p> <p>Class hierarchy overview::</p> <pre><code>HardwareModel\n \u2514\u2500 qubits: dict[str, Qubit]\n       \u251c\u2500 physical_channel: PhysicalChannel\n       \u2502    \u251c\u2500 baseband: BaseBand\n       \u2502    \u2514\u2500 iq_voltage_bias: IQVoltageBias\n       \u251c\u2500 pulse_channels: QubitPulseChannels\n       \u2502    \u251c\u2500 drive: DrivePulseChannel\n       \u2502    \u251c\u2500 second_state: SecondStatePulseChannel\n       \u2502    \u251c\u2500 freq_shift: FreqShiftPulseChannel\n       \u2502    \u251c\u2500 reset: ResetPulseChannel\n       \u2502    \u251c\u2500 cross_resonance_channels: dict[int, CrossResonancePulseChannel]\n       \u2502    \u2514\u2500 cross_resonance_cancellation_channels: dict[int, ...]\n       \u251c\u2500 resonator: Resonator\n       \u2502    \u251c\u2500 physical_channel: PhysicalChannel\n       \u2502    \u2514\u2500 pulse_channels: ResonatorPulseChannels\n       \u2502         \u251c\u2500 measure: MeasurePulseChannel\n       \u2502         \u251c\u2500 acquire: AcquirePulseChannel\n       \u2502         \u2514\u2500 reset: ResetPulseChannel\n       \u251c\u2500 x_pi_2_comp: XPi2Comp\n       \u2514\u2500 zx_pi_4_comp: dict[int, ZxPi4Comp]\n</code></pre>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.Component","title":"<code>Component</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Base class for schema objects that carry a UUID identifier.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <code>UUID</code> <p>Unique identifier for this component.  Auto-generated when not supplied, so calibration files without UUIDs are accepted.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.BaseBand","title":"<code>BaseBand</code>","text":"<p>               Bases: <code>Component</code></p> <p>Baseband oscillator parameters for a physical channel.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <code>UUID</code> <p>Unique identifier inherited from :class:<code>Component</code>.</p> <code>frequency</code> <code>float</code> <p>Baseband carrier frequency in Hz.</p> <code>if_frequency</code> <code>float</code> <p>Intermediate frequency in Hz.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.IQVoltageBias","title":"<code>IQVoltageBias</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>IQ voltage bias correction string for a physical channel.</p> <p>Attributes:</p> Name Type Description <code>bias</code> <code>str</code> <p>Serialised bias value (format is hardware-specific).</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.PhysicalChannel","title":"<code>PhysicalChannel</code>","text":"<p>               Bases: <code>Component</code></p> <p>Physical channel connecting a qubit or resonator to the control hardware.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <code>UUID</code> <p>Unique identifier inherited from :class:<code>Component</code>.</p> <code>name_index</code> <code>int</code> <p>Hardware name index.</p> <code>baseband</code> <code>BaseBand</code> <p>Associated :class:<code>BaseBand</code> oscillator.</p> <code>block_size</code> <code>int</code> <p>Waveform block size in samples.</p> <code>iq_voltage_bias</code> <code>IQVoltageBias</code> <p>IQ voltage bias correction.</p> <code>default_amplitude</code> <code>int</code> <p>Default output amplitude (hardware units).</p> <code>switch_box</code> <code>str</code> <p>Switch-box identifier string.</p> <code>swap_readout_iq</code> <code>bool</code> <p>Whether to swap I and Q for readout (resonator only).</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.CalibratablePulse","title":"<code>CalibratablePulse</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A parameterised pulse waveform used for qubit control.</p> <p>Attributes:</p> Name Type Description <code>waveform_type</code> <code>str</code> <p>Waveform shape identifier (e.g. <code>\"gaussian\"</code>).</p> <code>width</code> <code>float</code> <p>Pulse duration in seconds.</p> <code>amp</code> <code>float</code> <p>Pulse amplitude.</p> <code>phase</code> <code>float</code> <p>Pulse phase in radians.</p> <code>drag</code> <code>float</code> <p>DRAG correction coefficient.</p> <code>rise</code> <code>float</code> <p>Rise-time parameter as a fraction of width.</p> <code>amp_setup</code> <code>float</code> <p>Setup amplitude.</p> <code>std_dev</code> <code>float</code> <p>Gaussian standard deviation in seconds.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.PulseChannel","title":"<code>PulseChannel</code>","text":"<p>               Bases: <code>Component</code></p> <p>Base class for all pulse channels.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <code>UUID</code> <p>Unique identifier inherited from :class:<code>Component</code>.</p> <code>frequency</code> <code>float</code> <p>Carrier frequency in Hz (<code>NaN</code> when unset).</p> <code>imbalance</code> <code>float</code> <p>IQ imbalance correction factor.</p> <code>phase_iq_offset</code> <code>float</code> <p>IQ phase offset in radians.</p> <code>scale</code> <code>complex | float</code> <p>Complex or real scale factor applied to waveform amplitudes.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.DrivePulseChannel","title":"<code>DrivePulseChannel</code>","text":"<p>               Bases: <code>PulseChannel</code></p> <p>Drive pulse channel used to apply single-qubit gates.</p> <p>Attributes:</p> Name Type Description <code>pulse</code> <code>CalibratablePulse</code> <p>Primary X-\u03c0/2 drive pulse.</p> <code>pulse_x_pi</code> <code>CalibratablePulse | None</code> <p>Optional X-\u03c0 drive pulse (<code>None</code> when not calibrated).</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.QubitPulseChannel","title":"<code>QubitPulseChannel</code>","text":"<p>               Bases: <code>PulseChannel</code></p> <p>Marker base class for qubit-specific pulse channels.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.SecondStatePulseChannel","title":"<code>SecondStatePulseChannel</code>","text":"<p>               Bases: <code>QubitPulseChannel</code></p> <p>Pulse channel for driving the |1\u27e9\u2192|2\u27e9 transition.</p> <p>Attributes:</p> Name Type Description <code>active</code> <code>bool</code> <p>Whether second-state driving is enabled.</p> <code>delay</code> <code>float</code> <p>Delay before the pulse in seconds.</p> <code>pulse</code> <code>CalibratablePulse | None</code> <p>Optional second-state pulse waveform.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.FreqShiftPulseChannel","title":"<code>FreqShiftPulseChannel</code>","text":"<p>               Bases: <code>QubitPulseChannel</code></p> <p>Frequency-shift pulse channel for AC-Stark shift calibration.</p> <p>Attributes:</p> Name Type Description <code>active</code> <code>bool</code> <p>Whether frequency shifting is enabled.</p> <code>amp</code> <code>float</code> <p>Frequency-shift amplitude.</p> <code>phase</code> <code>float</code> <p>Frequency-shift phase in radians.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.CrossResonancePulseChannel","title":"<code>CrossResonancePulseChannel</code>","text":"<p>               Bases: <code>QubitPulseChannel</code></p> <p>Cross-resonance (CR) pulse channel for two-qubit gate calibration.</p> <p>Attributes:</p> Name Type Description <code>auxiliary_qubit</code> <code>int</code> <p>Index of the auxiliary (target) qubit.</p> <code>zx_pi_4_pulse</code> <code>CalibratablePulse | None</code> <p>Optional ZX-\u03c0/4 calibration pulse.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.CrossResonanceCancellationPulseChannel","title":"<code>CrossResonanceCancellationPulseChannel</code>","text":"<p>               Bases: <code>QubitPulseChannel</code></p> <p>Cross-resonance cancellation (CRC) pulse channel.</p> <p>Attributes:</p> Name Type Description <code>auxiliary_qubit</code> <code>int</code> <p>Index of the auxiliary (target) qubit.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.ResetPulseChannel","title":"<code>ResetPulseChannel</code>","text":"<p>               Bases: <code>PulseChannel</code></p> <p>Reset pulse channel for returning a qubit or resonator to the ground state.</p> <p>Attributes:</p> Name Type Description <code>delay</code> <code>float</code> <p>Delay before the reset pulse in seconds.</p> <code>pulse</code> <code>CalibratablePulse</code> <p>Reset pulse waveform.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.QubitPulseChannels","title":"<code>QubitPulseChannels</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Container for all pulse channels owned by a single qubit.</p> <p>Attributes:</p> Name Type Description <code>drive</code> <code>DrivePulseChannel</code> <p>Drive pulse channel.</p> <code>second_state</code> <code>SecondStatePulseChannel</code> <p>Second-state pulse channel.</p> <code>freq_shift</code> <code>FreqShiftPulseChannel</code> <p>Frequency-shift pulse channel.</p> <code>reset</code> <code>ResetPulseChannel</code> <p>Qubit reset pulse channel.</p> <code>cross_resonance_channels</code> <code>dict[int, CrossResonancePulseChannel]</code> <p>Mapping of auxiliary-qubit index to :class:<code>CrossResonancePulseChannel</code>.</p> <code>cross_resonance_cancellation_channels</code> <code>dict[int, CrossResonanceCancellationPulseChannel]</code> <p>Mapping of auxiliary-qubit index to :class:<code>CrossResonanceCancellationPulseChannel</code>.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.ResonatorPulseChannel","title":"<code>ResonatorPulseChannel</code>","text":"<p>               Bases: <code>PulseChannel</code></p> <p>Marker base class for resonator-specific pulse channels.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.MeasurePulseChannel","title":"<code>MeasurePulseChannel</code>","text":"<p>               Bases: <code>ResonatorPulseChannel</code></p> <p>Measure pulse channel that drives the readout resonator.</p> <p>Attributes:</p> Name Type Description <code>pulse</code> <code>CalibratablePulse</code> <p>Measurement pulse waveform.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.CalibratableAcquire","title":"<code>CalibratableAcquire</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Acquisition parameters for a readout resonator.</p> <p>Attributes:</p> Name Type Description <code>delay</code> <code>float</code> <p>Acquisition delay after the measurement pulse in seconds.</p> <code>width</code> <code>float</code> <p>Acquisition window width in seconds.</p> <code>sync</code> <code>bool</code> <p>Whether to synchronise acquisition across multiple channels.</p> <code>use_weights</code> <code>bool</code> <p>Whether to apply integration weights.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.AcquirePulseChannel","title":"<code>AcquirePulseChannel</code>","text":"<p>               Bases: <code>ResonatorPulseChannel</code></p> <p>Acquire pulse channel that controls the ADC acquisition window.</p> <p>Attributes:</p> Name Type Description <code>acquire</code> <code>CalibratableAcquire</code> <p>Acquisition parameters.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.MeasureAcquirePulseChannel","title":"<code>MeasureAcquirePulseChannel</code>","text":"<p>               Bases: <code>MeasurePulseChannel</code>, <code>AcquirePulseChannel</code></p> <p>Combined measure-and-acquire pulse channel retained for backwards compatibility.</p> <p>Earlier versions of the schema represented measurement and acquisition as a single combined channel.  The current schema separates them into :class:<code>MeasurePulseChannel</code> and :class:<code>AcquirePulseChannel</code> held inside a :class:<code>ResonatorPulseChannels</code> container.  This class is kept so that serialised payloads written against the old schema can still be deserialised; it should not be used for new code.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.ResonatorPulseChannels","title":"<code>ResonatorPulseChannels</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Container for all pulse channels owned by a single resonator.</p> <p>Attributes:</p> Name Type Description <code>measure</code> <code>MeasurePulseChannel</code> <p>Measurement pulse channel.</p> <code>acquire</code> <code>AcquirePulseChannel</code> <p>Acquisition pulse channel.</p> <code>reset</code> <code>ResetPulseChannel</code> <p>Resonator reset pulse channel.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.Resonator","title":"<code>Resonator</code>","text":"<p>               Bases: <code>Component</code></p> <p>Readout resonator coupled to a qubit.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <code>UUID</code> <p>Unique identifier inherited from :class:<code>Component</code>.</p> <code>physical_channel</code> <code>PhysicalChannel</code> <p>Physical channel connected to the resonator.</p> <code>pulse_channels</code> <code>ResonatorPulseChannels</code> <p>Resonator pulse channels (measure, acquire, reset).</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.XPi2Comp","title":"<code>XPi2Comp</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>X-\u03c0/2 gate phase compensation parameters.</p> <p>Attributes:</p> Name Type Description <code>phase_comp_x_pi_2</code> <code>float</code> <p>Phase compensation value in radians.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.ZxPi4Comp","title":"<code>ZxPi4Comp</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>ZX-\u03c0/4 gate compensation parameters for a single CR pair.</p> <p>Attributes:</p> Name Type Description <code>pulse_precomp_target_zx_pi_4</code> <code>CalibratablePulse | None</code> <p>Optional pre-compensation pulse.</p> <code>pulse_postcomp_target_zx_pi_4</code> <code>CalibratablePulse | None</code> <p>Optional post-compensation pulse.</p> <code>phase_comp_target_zx_pi_4</code> <code>float</code> <p>Target ZX-\u03c0/4 phase compensation in radians.</p> <code>pulse_zx_pi_4_target_rotary_amp</code> <code>float | None</code> <p>Optional rotary pulse amplitude.</p> <code>precomp_active</code> <code>bool</code> <p>Whether pre-compensation is enabled.</p> <code>postcomp_active</code> <code>bool</code> <p>Whether post-compensation is enabled.</p> <code>use_second_state</code> <code>bool</code> <p>Whether to use the second excited state.</p> <code>use_rotary</code> <code>bool</code> <p>Whether to use a rotary pulse.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.Qubit","title":"<code>Qubit</code>","text":"<p>               Bases: <code>Component</code></p> <p>Full calibration record for a single physical qubit.</p> <p>Attributes:</p> Name Type Description <code>uuid</code> <code>UUID</code> <p>Unique identifier inherited from :class:<code>Component</code>.</p> <code>physical_channel</code> <code>PhysicalChannel</code> <p>Physical channel connected to the qubit drive line.</p> <code>pulse_channels</code> <code>QubitPulseChannels</code> <p>All pulse channels owned by this qubit.</p> <code>resonator</code> <code>Resonator</code> <p>Coupled readout resonator.</p> <code>mean_z_map_args</code> <code>list[complex | float]</code> <p>Two-element list <code>[real, imag]</code> encoding the mean-Z state-discrimination mapping arguments.</p> <code>discriminator</code> <code>complex | float</code> <p>Complex or real threshold for state discrimination.</p> <code>direct_x_pi</code> <code>bool</code> <p>Whether to use a direct X-\u03c0 pulse instead of two X-\u03c0/2 pulses.</p> <code>x_pi_2_comp</code> <code>XPi2Comp</code> <p>X-\u03c0/2 gate phase compensation.</p> <code>zx_pi_4_comp</code> <code>dict[int, ZxPi4Comp]</code> <p>Mapping of auxiliary-qubit index to :class:<code>ZxPi4Comp</code> compensation data.</p>"},{"location":"schemas/hardware_model/#qupboard_graphql.schemas.hardware_model.HardwareModel","title":"<code>HardwareModel</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Top-level hardware model calibration record.</p> <p>This is the root schema object serialised to/from JSON for the REST API and mapped to/from the :class:<code>~qupboard_graphql.db.models.HardwareModelORM</code> tree in the database.</p> <p>Attributes:</p> Name Type Description <code>version</code> <code>str</code> <p>Schema or calibration version string.</p> <code>logical_connectivity</code> <code>dict[str, list[int]]</code> <p>Mapping of qubit label to list of neighbour indices describing the device topology.</p> <code>calibration_id</code> <code>str</code> <p>Identifier for the calibration run.</p> <code>qubits</code> <code>dict[str, Qubit]</code> <p>Mapping of qubit key (e.g. <code>\"q0\"</code>) to :class:<code>Qubit</code>.</p>"}]}